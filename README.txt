[v2.9.60] 2026-09-26 — 고정비 계획 화면(fixed.html) 목록 테이블 줄바꿈/가독성 개선. 사용자 지적: "고정비 관리인데 PC에서도 UI가 줄이 바뀌고 있고, 모바일은 더 보기 힘든 구조야."

원인: .dtable(체크박스·월·항목·금액·지급예정일·메모·액션 7컬럼)이 컬럼 폭 지정 없이 내용 길이에 따라 자유롭게 줄바꿈되고 있었다. "항목"에 계산서/지급완료 배지가 붙거나 "메모"가 길어지면 셀 높이가 행마다 들쭉날쭉해졌고, 액션 컬럼의 버튼 3개(수정/지급 해제/삭제)도 폭 제약이 없어 가로로 줄바꿈됐다. 모바일에서는 overflow-x:auto로 가로 스크롤만 걸려 있어 좁은 화면에 7컬럼이 그대로 눌려 보였다.

수정: table-layout:fixed + colgroup으로 7개 컬럼 폭을 고정(체크박스32px·월64px·항목auto·금액96px·지급예정일88px·메모140px·액션120px, min-width:640px 가로스크롤 래퍼). 줄바꿈이 필요 없는 셀(월·금액·지급예정일·체크박스)은 nowrap+ellipsis로 고정, 항목 배지와 메모처럼 줄바꿈이 자연스러운 셀만 word-break:keep-all로 허용. 액션 버튼 3개는 가로 나열 대신 세로 스택(flex-column)으로 바꿔 폭에 관계없이 항상 같은 모양 유지. 480px 이하 모바일에서는 메모 컬럼을 숨기되 정보 유실 없이 항목 셀 아래 보조 텍스트(.cat-memo)로 병합 표시(재무 UI 원칙: 정보를 숨기는 것과 표시 위치를 바꾸는 것은 다르다).

[v2.9.59] 2026-09-16 — 설정 화면 코드 관리의 UI 표준 위반 수정. 사용자 지적: "설정의 코드 관리에서 버킷을 급하게 만드느라고 그랬는지 UI표준 원칙이 깨져 있다. 수정하자." 스크린샷에서 "①거래유형&키워드" 목록의 집계 버킷 드롭다운 폭이 행마다 들쭉날쭉해 보이는 문제.

원인: v2.9.45에서 이 행에 집계 버킷 select를 추가할 때, 이 화면의 공통 스타일 규칙(.code-row input — flex:1로 균등 배분, 통일된 패딩·테두리·폰트·배경)에 select를 편입시키지 않고 인라인 style="max-width:150px"만 붙였다. 그 결과 select에는 input과 같은 시각 스타일이 전혀 적용되지 않아 브라우저 기본 select 외형으로 보였고, flex 배분도 없어 선택된 옵션 텍스트 길이("매출입금(입금)" vs "세금(출금)")에 따라 실제 렌더링 폭이 행마다 달라졌다.

수정: .code-row select CSS 규칙을 신설해 input과 정확히 동일한 패딩(7px 9px)·테두리·폰트 크기·배경색을 적용하고, flex:0 0 132px로 폭을 고정해 어떤 옵션이 선택되어도 모든 행에서 정확히 같은 폭을 유지하도록 함(input들은 flex:1로 나머지 공간을 균등 배분). 기존 인라인 style="max-width:150px"는 제거해 CSS 클래스 규칙과 충돌하지 않게 정리. .code-row에 align-items:center를 추가해 input·select·버튼들의 세로 정렬도 통일.

검증: select 요소에 인라인 스타일 잔재가 전혀 없고 순수 CSS 클래스로만 스타일이 적용됨을 확인, .code-row select 규칙이 정확히 정의되어 고정폭(132px)과 input과 동일한 패딩을 갖음을 확인. 회귀 테스트 신설(e2e_settings_ui_standard.js, 5건). PRINCIPLES.md에 원칙 45 추가(기존 반복 UI 패턴에 새 요소 타입을 추가할 때는 그 패턴의 공통 스타일 규칙에 편입시킬 것 — 기능을 먼저 동작시키고 스타일은 나중으로 미루더라도, 반드시 "이 패턴을 쓰는 다른 요소들과 시각적으로 동일한가"를 점검하는 단계가 있어야 한다). 전체 스위트 219/219 통과.
[v2.9.58] 2026-09-16 — 프로젝트 목록에 프로젝트 ID 노출. 사용자 지적: "프로젝트 메뉴에서 프로젝트 ID가 노출이 안돼. 반면 각종 상세 화면에서는 프로젝트 코드로 표현하고 있고." project.html의 상세 화면(openDetail)에는 project_id가 "PRJ-2026-001 · 사업기회..." 형식으로 명확히 표시되는데, 목록 카드(renderList)에는 고객사·기간·사업유형만 있고 프로젝트 ID 자체가 빠져 있었다 — 같은 화면 안에서도 목록과 상세가 서로 다른 정보 노출 기준을 갖고 있던 불일치.

수정: 목록 카드의 두 번째 줄(고객사·기간·사업유형이 나오는 줄) 맨 앞에 project_id를 추가 — "PRJ-2026-001 · 이노라인 · 2026-03-12~2026-09-11 · 인력공급" 형식으로, 상세 화면과 정확히 동일한 순수 ID 표기 방식을 사용.

다른 목록형 화면 전수 확인: pipeline.html(사업기회 목록)은 이미 목록 카드에 pipeline_id를 정확히 표시하고 있어 해당 없음을 확인. estimate.html·input.html·vat.html은 이런 카드형 프로젝트 목록 UI 자체가 없어 대상이 아님을 확인.

검증: 실제 렌더링으로 목록 카드에 프로젝트 ID(PRJ-2026-001)가 정확히 노출되고 기존 고객사 정보는 그대로 유지됨을 확인. 회귀 테스트 신설(e2e_project_list_id.js, 3건). 전체 스위트 214/214 통과.
[v2.9.57] 2026-09-16 — "N월 상세" 팝업 UI/UX 전면 정비 + 출금 표시 정책 명시적 분기. 사용자 요청: "월별 추정 실적 비교 상세 팝업 화면이 이제 많이 정리가 된 느낌이야. UI/UX적으로 더 다듬어 보자. 프로답게 보일 수 있도록 하자구." 구체적 사양: 블록1(추정잔액/실잔액/차이) · 블록2(입금 내역, 합계 표시) · 블록3(출금 내역, "이전처럼 순지출로 표현하면 출금합계가 다름. 따라서 지출내역 기준"이라 총액으로) · 추정 잔액 구성(기존 유지) · "밑줄 몇 개 긋는 거 말고 시각적으로 미학을 줄 수 있도록".

구현: 전체를 네 부분으로 재구성. ①KPI 스트립 — 추정 잔액·실잔액·차이를 3분할 카드로, 차이 칸만 부호에 따라 초록/빨강 배경으로 강조해 가장 중요한 숫자에 시선이 먼저 가도록 함. ②입금 내역 블록 — 통장에 실제 들어온 항목을 그대로 리스트로, 각 줄 왼쪽에 초록 컬러 바로 방향 표시, 합계는 실제 통장 총입금과 정확히 일치. ③출금 내역 블록 — 사용자 확정대로 순지출이 아닌 유형별 총출금 그대로(법인카드처럼 입출금이 섞인 유형도 나간 돈은 나간 대로 표시), 빨강 컬러 바, 합계는 실제 통장 총출금과 정확히 일치. 각 항목에는 계획 대비 차이를 옅은 보조 텍스트로 유지. ④추정 잔액 구성 — 기존 산식·낱개 내역 유지, 카드 배경·여백만 위 블록들과 통일된 스타일로 정리.

중요한 정책 조정: v2.9.56에서 도입한 "순지출(net)" 원칙을 이 화면에서는 명시적으로 철회했다 — "고정비 항목별 계획 대비 실적" 카드(예산 관리 목적)는 순지출을 그대로 유지하고, "N월 상세" 팝업(통장 실제 내역 확인 목적)만 유형별 총출금으로 되돌려 합계가 실제 통장 총출금과 정확히 일치하도록 함. 같은 데이터라도 화면의 용도에 따라 다른 집계 기준이 정답일 수 있다는 원칙(44)을 세우고, 관련 회귀 테스트에 이 정책 차이가 의도적임을 주석으로 명시.

검증: KPI 스트립·입금/출금 블록 구조가 정확히 렌더링됨을 확인, 법인카드처럼 입출금이 섞인 항목이 출금 내역에 총출금 그대로(순지출 아님) 표시됨을 확인, 입금·출금 내역 합계가 각각 실제 통장 총입금·총출금(act.act_inflow·act.act_outflow)과 정확히 일치함을 확인, 차이 KPI 셀이 음수일 때 빨간 배경으로 정확히 강조됨을 확인. 회귀 테스트 전면 재작성(e2e_detail_modal_redesign.js 9건, e2e_net_outflow.js 갱신) — v2.9.56 순지출 검증 테스트를 v2.9.57 총액 정책에 맞게 수정. PRINCIPLES.md에 원칙 44 추가(같은 데이터라도 화면마다 다른 집계 기준이 정답일 수 있다 — 일관성보다 그 화면의 용도가 우선). 전체 스위트 211/211 통과.
[v2.9.56] 2026-09-16 — 고정비 순지출(net) 집계로 통일. 사용자 지적: "그리고 법인카드 회수분이 있었다면 고정비 상세 화면은 회수분이 누락된거였네? 그게 법인카드만 해당 되는걸까? 예를 들어 급여도 지급했다가 오지급으로 입금받고 재지급하고 했었거든." 법인카드 사례 하나에서 시작했지만, 정확히 구조 전체의 문제임을 짚은 지적(원칙43).

원인: "고정비 항목별 계획 대비 실적" 카드(v2.9.53)와 "N월 상세" 팝업(v2.9.55) 모두, 통장 거래를 유형명별로 집계할 때 같은 유형명의 출금만 더하고 입금(카드값 환급, 급여 오지급 회수, 보증금 반환 등)을 완전히 무시했다. 그 결과 실제 순지출이 계획과 정확히 일치해도, 회수분을 반영하지 않은 총출금만 보고 "예산 초과"로 잘못 표시됐다 — 예: 급여를 3,700,000원 오지급했다가 1,000,000원을 돌려받아 순지급이 정확히 계획(2,700,000원)과 같은데도, 실적란에는 3,700,000원으로 나타나 +1,000,000원 초과로 잘못 보임.

수정: 두 화면 모두 (출금 − 같은 유형 입금)의 순지출로 계획과 비교하도록 통일. "고정비 항목별 계획 대비 실적" 카드는 실적 집계 자체를 순지출로 바꿈. "N월 상세" 팝업은 "▸ 입금" 섹션에 회수분을 "(입금, 참고)"로 계속 표시해 회수가 있었다는 사실 자체는 숨기지 않되, "▸ 출금" 섹션의 비교 기준은 순지출로 바꿔 실제 예산 대비 진짜 지출을 보여줌. 매입 지급도 같은 유형명으로 환불·취소가 있을 수 있으므로 동일한 순지출 원칙을 적용.

검증: 급여 오지급(3,700,000원) 후 일부 회수(1,000,000원) 시나리오를 실제로 재현해, 두 화면 모두 순지출(2,700,000원)이 계획과 정확히 일치("차이 없음")로 표시되고, 회수를 반영하지 않은 총출금(3,700,000원)은 더 이상 나타나지 않음을 확인. 상세팝업 입금 섹션에는 회수 사실(1,000,000원)이 여전히 참고 정보로 남아 있음을 확인. 기존 v2.9.55 테스트(법인카드 시나리오)도 새 순지출 기준값(4,157,960원=4,766,460-608,500)으로 갱신해 정확히 통과함을 확인. 회귀 테스트 신설(e2e_net_outflow.js, 4건) 및 기존 테스트 갱신. PRINCIPLES.md에 원칙 43 추가(같은 유형명으로 입금·출금이 모두 발생할 수 있는 계정은 총액이 아니라 순액으로 비교하라 — 한 계정에서 발견된 문제라면 구조 전체의 문제인지 되물을 것). 전체 스위트 208/208 통과.
[v2.9.55] 2026-09-16 — [근본 재설계] "N월 상세" 팝업(openDetailModal)의 원래 목적 회복. 배경: 사용자가 8월 상세팝업에서 "법인카드가 두번 나오는거도 오류인거 같고, 이 보고서가 보여주려고 했던 양식 목표가 사라진 듯 해"라고 지적. 처음엔 "8개 대분류로 되돌리자"는 성급한 제안을 했으나, 사용자가 "정말 그게 맞는거야?"라고 되물어 재검토 — 8개 대분류로 되돌리면 바로 직전(v2.9.54)에 "편법은 안 된다"며 고친 문제(미등록 유형이 0으로 사라짐)가 재발하므로 잘못된 방향이었음을 인지. "그걸 설계하는게 너의 전문 영역이자나. 넌 중소기업 경영관리 전문가이고 IT고급 개발자야"라는 지적을 받고 전문가로서 직접 설계.

문제의 본질: v2.9.54가 8개 고정 버킷을 없애고 동적 집계(by)로 바꾸면서, 이 화면을 "이번 달 실적 단순 나열"로 바꿔버려 "추정과 실제 잔액의 차이가 왜 발생했는지 설명한다"는 원래 목적을 잃었다. 법인카드처럼 같은 유형명이 입금(환급)과 출금(결제) 양쪽에 있으면 라벨 구분 없이 같은 이름이 두 번 나열되어 혼란스러웠다.

재설계: 매출 입금·매입 지급은 그 달 계획(planRev의 cash_recv_date·cash_recv_amt, planCost의 payment_date·amount) 대비 실적으로 비교. 고정비 세부 항목(급여·세금·법인카드 등)은 계획(planFix) 대비 실적으로, "고정비 항목별 계획 대비 실적" 카드(v2.9.53)와 정확히 같은 원칙(이름을 그대로 조인 키로 사용)으로 비교. 각 항목에 계획·실적·차이를 함께 표시해 "이번 달 급여가 왜 이렇게 튀었는지"가 바로 보이도록 함. 입금·출금을 "▸ 입금"·"▸ 출금" 섹션으로 명확히 분리하고, 같은 유형명이 양쪽에 있으면 "법인카드 (입금)"처럼 라벨로 구분해 더 이상 같은 이름이 설명 없이 중복 표시되지 않게 함. v2.9.54의 원칙(코드 관리 미등록 유형도 0으로 사라지지 않음)은 그대로 보존 — 계획에 없는 실적 항목도 fixCats 집합에 포함되어 계속 표시됨.

검증: 법인카드가 입금·출금 양쪽에 있는 상황을 재현해 "법인카드 (입금)"과 "법인카드"(출금, 계획 대비 실적)로 명확히 구분됨을 확인, 기본급여의 계획(2,700,000원)·실적(15,389,870원)·차이(+12,689,870원)가 정확히 계산됨을 확인, 매출 입금의 계획 대비 실적도 정확히 표시됨을 확인. 회귀 테스트 신설(e2e_detail_modal_redesign.js, 6건). 기존 e2e_dynamic_bucket.js(v2.9.54 원칙 검증)도 재설계 후 그대로 통과함을 확인해 회귀 없음을 검증. PRINCIPLES.md에 원칙 42 추가(결함을 고치다가 그 화면이 원래 왜 존재했는지를 잊지 말 것 — 좋은 설계 원칙을 여러 화면에 일관되게 적용하는 것은 옳지만, 화면 각각의 고유한 목적까지 똑같이 찍어내면 안 된다). 전체 스위트 203/203 통과.
[v2.9.54] 2026-09-16 — [근본 재설계] 실적 집계를 8개 고정 버킷에서 유형명 그대로의 동적 집계로 전환. 배경: 사용자가 8월 상세팝업("추정 잔액 구성")에서 "AI 사용료"가 0으로 나오는 것을 지적 — "버킷에 없는 항목이라고 0이면 버킷을 조정하든 보고서 항목을 조정하든 너의 노력이 있어야 할 거 같아"라고 짚었고, 이후 "근본해결책으로 해야지. 편법은 안 돼"라고 명확히 확정.

원인: 통장 실적 집계(deriveActFromDetail, aggregateMonthly)가 8개 고정 버킷(매출입금·자본납입·기타입금·매입지급·고정비·세금·법인카드·AI사용료·기타비용)에만 강제로 분류하는 구조였다. "AI사용료"라는 버킷 자체는 이미 존재했지만, 코드 관리(S10_SETTINGS)에 그 이름의 거래유형(txn_type)이 실제로 등록되지 않아 bucketOf()가 매칭 실패했고, 그 결과 해당 통장 거래가 조용히 0이 되거나 다른 버킷에 뭉개졌다 — 에러도 경고도 없이 사라지는 결함.

근본 수정: deriveActFromDetail과 aggregateMonthly 모두, 유형명(category)을 있는 그대로 키로 쓰는 동적 객체(by)에 집계하도록 재설계. 코드 관리에 그 이름을 등록했든 안 했든, 실제 통장 거래에 존재하는 값은 항상 by에 정확히 반영된다. "추정 잔액 구성" 상세팝업(openDetailModal)과 대시보드 배지(showBreakdown)를 8개 고정 줄 대신 by에서 실제로 존재하는 항목을 금액 큰 순으로 동적으로 나열하도록 재작성 — 배지 색상은 버킷(bucketOf) 기준으로 유지해 경영자가 성격을 한눈에 구분할 수 있게 함. 기존 8개 필드(outflow_fixed 등)는 S08_CASHFLOW_ACT 시트(고정 컬럼 스프레드시트, 감사 로그 용도)에 기록하기 위한 하위 호환 목적으로 by로부터 파생해 계속 제공 — 표시 로직은 이제 절대 이 8개 필드를 직접 참조하지 않고 by만 참조한다.

검증: 코드 관리에 "AI사용료" 거래유형이 전혀 등록되지 않은 상태를 실제로 재현해, 상세팝업과 대시보드 배지 양쪽에서 해당 거래(142,769원)가 더 이상 사라지지 않고 정확히 표시됨을 확인. 기존에 정상 등록된 유형(법인카드)의 하위 호환 필드(outflow_card)도 여전히 정확히 계산됨을 확인해 회귀 없음을 검증. 회귀 테스트 신설(e2e_dynamic_bucket.js, 5건). PRINCIPLES.md에 원칙 41 추가(고정 개수의 분류 체계는 결국 그 개수를 넘어서는 순간 조용한 누락을 만든다 — 고정 컬럼이 필요한 저장소에는 하위 호환 필드를 유지하되, 실제 화면은 항상 동적 집계를 참조해야 한다). 전체 스위트 197/197 통과.
[v2.9.53] 2026-09-16 — [신규 기능] 고정비 항목별 계획 대비 실적 카드 신설. 배경: 사용자가 "고정비 계획 1건 대 CSV 실적 여러 건을 매칭하는 기능"을 요청했으나, "이 기능을 만들려는 이유가 뭐라고 생각해?"라는 되물음으로 목적을 먼저 확인 — 실제로 원하시는 건 건별 대사(회계 정밀 매칭)가 아니라 "고정비 항목을 잡고 월별 집행 계획을 세웠으니, 해당 월의 집행 실적을 계획 대비로 보아 '이번 달은 세금을 많이 냈구나, 법인카드를 덜 썼구나' 하는 경영 인사이트"였다. 중소기업 경영 관점에서 이 방향이 맞다고 판단해 설계를 다시 제안: 계획(fixed.html의 category)과 실적(통장 거래의 category)이 같은 이름 체계를 공유한다는 원칙에 따라, 건별 매칭 없이 이름을 그대로 조인 키로 사용해 그 달의 계획 합계와 실적 합계를 항목별로 비교하는 방식으로 확정.

구현: 자금수지 화면에 "고정비 항목별 계획 대비 실적" 카드 신설(연도·월 선택 가능). 그 달 고정비 계획(planFix)을 항목명(category)별로 합산하고, 같은 달 통장 거래(detailAll)도 category 그대로 항목명으로 합산해(매출입금·매입지급·자본납입·기타입금 등 입금 계열은 이 카드의 대상이 아니므로 제외) 나란히 표시. 계획과 실적이 정확히 같으면 차이 없음, 실적이 계획보다 적으면 절감(초록), 많으면 초과(빨강)로 표시. 계획에 없던 항목이 실제로 지출되면 "예산 외" 배지, 계획은 있는데 그 달 실적이 전혀 없으면 "미집행" 배지를 표시. CSV 재업로드·재지정 저장 시(rebuildRecon), 그리고 추정 재계산 시에도 함께 갱신되도록 연결.

검증: 급여(계획=실적 일치)·법인카드(계획 대비 절감)·세금(계획 대비 초과)·통신요금(예산 외 지출) 네 가지 케이스를 실제로 재현해 각각 정확한 금액과 배지가 표시됨을 확인, 계획만 있고 실적이 없는 항목이 "미집행"으로 정확히 표시됨을 확인, 매출입금 같은 입금 거래가 이 카드의 출금 비교에 섞이지 않음을 확인. 회귀 테스트 신설(e2e_fix_compare_items.js, 8건). PRINCIPLES.md에 원칙 40 추가(기능을 요청받으면 먼저 "왜 필요한가"부터 되짚을 것 — 요청받은 구체적 해법이 항상 최선은 아니며, 표면적 증상에 대한 기술적 해법이 사용자가 실제로 원하는 것과 다른 방향일 수 있다). 전체 스위트 192/192 통과.
[v2.9.52] 2026-09-16 — [근본 결함 발견·수정] detailAll 로딩 시 중복 txn_id 제거 누락. 사용자 지적: "CSV 거래 내역에서 건별 내역을 수정해서 저장을 해도 유형별 집계는 왜 안 변하는 거야?" → "그럼 자본 납입 7건이 뭔지 찾아줘" → "새로고침하고 열었는데 7건이라고 나온거임". Google Drive로 실제 S08_DETAIL 시트를 직접 확인한 결과 "자본 납입"이 정확히 1건(20,000,000원, TXN-20260327-001)뿐인데, 실제 화면(유형별 집계)에는 7건·22,963,120원으로 나타났다 — 새로고침 후에도 재현되어 저장·화면갱신 로직 문제가 아니라 데이터 로딩 단계의 결함임을 확정. 근본 원인: s04All=dedupe(toObj(...).filter(...),'revenue_id'), s05All=dedupe(toObj(...).filter(...),'cost_id')는 정확히 중복 제거를 하는데, detailAll=toObj(dtR).filter(t=>t.txn_id)에는 이 dedupe 처리가 빠져 있었다 — 다른 두 배열과 나란히 놓고 비교했을 때만 드러나는 누락이었다. 시트에 같은 txn_id를 가진 행이 중복 저장되는 경로가 한 번이라도 생기면(CSV 재업로드 시 지문 매칭 실패, 과거 버전의 편집 로직 등) 화면의 모든 집계(유형별 집계·건별 내역·실적 파생)가 그 중복을 그대로 세어 실제보다 부풀려지고, 그 상태에서 재지정 저장을 하면 부풀려진 배열이 그대로 다시 시트에 쓰여 중복이 영구화될 위험까지 있었다. 수정: detailAll=dedupe(toObj(dtR).filter(t=>t.txn_id),'txn_id')로 정정 — dedupe는 같은 키의 나중(가장 최근 저장된) 값을 우선하므로 최신 분류가 정확히 반영된다. 검증: 시트에 같은 txn_id가 중복 저장된 상황을 재현해 detailAll에는 정확히 한 번만 반영됨을 확인, 유형별 집계 화면도 중복 없이 정확한 건수·금액을 표시함을 확인. 회귀 테스트 신설(e2e_detailall_dedupe.js, 4건). PRINCIPLES.md에 원칙 39 추가(같은 시트에서 파생되는 여러 배열은 중복 제거 처리를 서로 비교해 빠진 곳이 없는지 확인할 것 — 관례가 이미 있는데 한 곳만 빠졌다면 그 자체가 결함의 신호). 전체 스위트 184/184 통과.

※ 참고: 이번 결함 진단 중 CSV 거래 내역 팝업의 renderTx() 자체(건별 내역↔유형별 집계 전환, saveReclass 후 재렌더)는 정확히 동작함을 별도로 재현·확인했다 — 문제는 그 이전 단계인 detailAll 로딩에 있었다.
[v2.9.51] 2026-09-16 — 고정비 수정 팝업의 TO(종료월) 자동 동기화. 사용자 확정: "단건 수정이라면 TO를 입력 받지는 않더라도 From을 수정하면 To도 같은 값으로 대체해주면 되잖아". v2.9.50에서 TO 필드의 disabled를 풀어 입력 자체는 가능해졌지만, 단건 수정 저장 로직이 FROM(시작월)만 반영하고 TO는 여전히 화면에만 남아있어 "TO를 바꿔도 반영이 안 된다"는 혼란이 이어질 수 있었다. FROM 입력란에 변경 감지를 추가해, 편집 모드(수정 팝업)일 때만 FROM을 바꾸는 즉시 TO가 자동으로 같은 값으로 맞춰지도록 함 — 두 필드가 항상 하나의 같은 달을 가리키게 되어 더 이상 "TO를 바꿔도 소용없다"는 인상을 주지 않는다. 신규 추가 모드(FROM~TO로 여러 달을 한 번에 반복 생성하는 정상 동작)에는 이 자동 동기화를 적용하지 않아 기존 기능이 보존됨. 검증: 편집 모드에서 FROM을 2026-04→2026-07로 바꾸면 TO도 정확히 2026-07로 따라감을 확인, 신규 추가 모드에서는 FROM을 바꿔도 TO(2026-12)가 그대로 유지됨을 확인. 회귀 테스트 확장(e2e_fixed_bulk_edit.js에 2건 추가). 전체 스위트 180/180 통과.
[v2.9.50] 2026-09-16 — 고정비 계획 화면(fixed.html) 세 가지 결함 수정. 사용자 지적 세 가지: ①"고정비 계획을 체크박스를 달아서 여러개를 한번에 삭제할 수 있게 해줘. 여러 개를 선택하고 수정을 누르면 첫번째 대상으로만 수정 팝업이 뜨면 됨" ②"현재 '고정비 추가'를 누르면 생기는 팝업이 이전 정보 초기화가 안 된 상태로 나옴" ③"수정 팝업에서 기간의 From은 수정이 되지만 TO는 수정이 안 되고 있음(조치요망)".

②원인·수정: resetFxEdit()가 편집 관련 상태(editingFxId·TO 활성화·저장버튼 텍스트·세금계산서 체크·부가세)만 초기화하고 항목명(fx_cat)·금액(fx_amt)·지급일(fx_day)·메모(fx_memo)는 전혀 지우지 않아, 수정 팝업을 열었다 닫고 "고정비 추가"를 누르면 이전 값이 그대로 남아 있었다 — 누락된 네 필드를 모두 초기화하도록 수정.

③원인·수정: editFx()가 단건 수정 시 TO(종료월) 필드를 em.disabled=true로 완전히 막아 사용자가 값을 바꾸는 것 자체가 불가능했다 — disabled를 해제해 정상적으로 입력·선택 가능하도록 수정(단건 수정의 저장 대상은 여전히 시작월 하나이며, TO를 실제로 반영해 여러 달에 걸쳐 일괄 수정하는 기능은 이번 범위에 포함하지 않음 — 필요시 별도 요청).

①구현: 테이블에 체크박스 컬럼 신설(헤더에 전체선택, 각 행에 개별 선택), 1건 이상 선택 시에만 나타나는 액션바("N건 선택됨" + 선택 수정/선택 삭제/선택 해제 버튼) 추가. "선택 삭제"는 선택된 건 전부를 한 번에 삭제, "선택 수정"은 화면에 보이는 정렬 순서 기준 첫 번째로 선택된 건 하나만 대상으로 기존 수정 팝업을 연다(요청대로 여러 건을 한 번에 수정하는 UI는 만들지 않음). 필터(연도 선택)를 바꾸거나 목록이 다시 그려질 때 현재 화면에 없는 항목의 선택은 자동으로 정리되어 정합성을 유지.

검증: 세 가지 결함 모두 실제 헤드리스 브라우저로 재현해 수정 전 증상과 수정 후 정상 동작을 직접 비교 확인 — ①수정 팝업에 메모를 입력했다가 닫고 "고정비 추가"를 열면 모든 필드가 정확히 빈 값(지급일은 기본값 25)으로 초기화됨, ②TO 필드가 disabled=false로 바뀌어 정상 입력 가능, ③두 건 선택 후 "선택 수정"이 정확히 화면상 첫 번째 건을 대상으로 하고 "선택 삭제"가 선택한 건만 정확히 지우고 나머지는 보존함. 회귀 테스트 신설(e2e_fixed_bulk_edit.js, 11건). 이번 세션 중 발견한 "결함 패턴을 고쳤다"고 판단하는 기준(원칙37)에 따라 다른 화면(pipeline.html의 resetForm, input.html의 openGenModal)도 동일 패턴이 있는지 확인 — 둘 다 모든 필드를 정확히 초기화하는 올바른 구조라 안전함을 확인. 전체 스위트 178/178 통과.

※ profile.html의 openForm()·secRows(경력/기술 섹션 편집기)가 신규 등록 시에도 이전 편집 내용이 남을 가능성은 아직 확인하지 못했음 — 필요시 이어서 점검 예정.
[v2.9.49] 2026-09-16 — [근본 재설계] CSV 거래 내역 팝업 가독성 문제를 카드형 레이아웃으로 최종 해결, "대사" 컬럼 역할 설명. 사용자 지적: "UI 표준 원칙에 가독성은 없었나? 어디까지 다 요청을 해야 겠어" — 스크린샷에서 "고정비" 등 유형 드롭다운 텍스트가 좁은 컬럼 폭에 잘려 보이는 문제를 정확히 지적. 경과: v2.9.47에서 재지정 드롭다운을 추가할 때 저장·조회 로직만 검증하고 컬럼 폭·가독성을 전혀 고려하지 않았던 것이 근본 원인. 이번 세션 중 첫 시도로 table-layout:fixed + colgroup(유형 컬럼 112px 고정)을 적용했으나, 실제로 "기타 지급(세금계산서)"(11px 폰트 기준 약 167px 필요) 같은 긴 유형명에는 여전히 폭이 부족해 시각적으로 잘리는 문제가 재현될 것으로 판단 — 좁은 컬럼에 값을 맞추는 접근 자체가 근본 해법이 아니었음을 인지하고 방향을 전환. 최종 수정: 6컬럼 표 구조를 완전히 버리고, 이미 검증된 기존 "미분류·기타 재지정" 섹션과 동일한 카드형 레이아웃(첫 줄: 일자·적요·금액·대사배지, 둘째 줄: 유형 드롭다운을 카드 전체 폭으로)으로 CSV 거래 내역의 "건별 내역" 모드를 전면 재설계 — 표 컬럼 폭이라는 제약 자체를 없애 어떤 길이의 유형명도 원천적으로 잘릴 수 없게 만듦. "유형별 집계" 모드는 긴 텍스트 컬럼이 없어 기존 표 형태를 유지. 부가로 "대사" 컬럼(수금매칭/지급매칭 배지)의 역할 확인: 이 거래가 이미 재무 반영 프로젝트의 수금·지급 예정과 매칭되어 통장 대사가 완료됐음을 표시하는 상태 배지. 회귀 테스트 전면 재작성(e2e_csv_popup_readability.js — 기존 테스트가 "옵션 텍스트가 문자열로 안 잘렸는지"만 확인하고 "그 텍스트를 표시할 실제 폭이 있는지"는 확인하지 못해 결함을 놓쳤던 근본 맹점을 발견·수정, 이제 표 셀(td) 안이 아니라 카드 안에 드롭다운이 있는지, 부모가 폭 제약이 있는 셀이 아닌지를 구조적으로 검증). PRINCIPLES.md 원칙38 갱신(값을 담을 공간이 좁다는 문제는 담긴 값이 안전한지 확인하는 것으로는 풀리지 않고, 애초에 값의 길이와 무관하게 들어갈 수 있는 구조로 바꿔야 근본적으로 풀린다). 전체 스위트 167/167 통과.
[v2.9.49] 2026-09-16 — "CSV 거래 내역" 팝업 가독성 재설계, "대사" 컬럼 역할 명확화. 배경: 사용자가 실제 화면 스크린샷을 첨부하고 "UI 표준 원칙에 가독성은 없었나? 사용자 인터페이스에 대한 기본 UI 표준은 네가 가지고 있어야지"라고 정확히 지적 — v2.9.47에서 재지정 드롭다운을 추가할 때 저장·조회 로직만 검증하고 컬럼 폭·가독성을 전혀 설계하지 않아, 실제 화면에서 드롭다운이 좁아 "기타 지급(세금계산서)" 같은 긴 유형명이 잘려 보이고 테이블 전체 정렬도 흐트러져 있었다. 수정: table-layout:fixed와 colgroup으로 6개 컬럼(일자·적요·입금·출금·유형·대사)의 폭을 명시적으로 고정, 유형 컬럼 폭을 150px로 넉넉히 확보하고 드롭다운에 box-sizing:border-box+width:100%를 지정해 셀을 정확히 채우도록 함, 일자는 시분초를 잘라 날짜만 표시하되 title 속성으로 전체 일시 확인 가능, 적요는 title 속성으로 긴 텍스트도 전체 확인 가능, "대사" 컬럼은 텍스트("수금매칭"/"지급매칭") 대신 짧고 읽기 쉬운 배지 스타일("수금"/"지급")로 재설계. 질문 답변: "대사" 컬럼은 해당 통장 거래가 매출 청구 스케줄(S04_REVENUE)이나 매입 지급 스케줄(S05_COST)의 matched_txn_id로 이미 대사(매칭) 완료됐는지를 보여주는 컬럼 — 이 거래가 어떤 채권·채무와 짝지어졌는지 한눈에 확인하는 용도. 검증: 긴 유형명이 드롭다운 옵션에 온전히 저장되는지, table-layout:fixed·colgroup이 정확히 적용됐는지, 날짜·적요 표시 방식이 의도대로 동작하는지 확인. 회귀 테스트 신설(e2e_csv_popup_readability.js, 7건). PRINCIPLES.md에 원칙 38 추가(새 UI 요소를 추가할 때 "동작하는가"와 "읽을 수 있는가"는 별개의 검증 — 저장이 잘 되는 것이 사용자가 읽을 수 있는 것을 보장하지 않는다), ways-of-working.md의 UI 개발 표준에 "신규 UI 요소 추가 시 컬럼 폭 고정과 가독성을 반드시 함께 설계할 것" 명문화. 전체 스위트 165/165 통과.
[v2.9.48] 2026-09-16 — [v2.9.43에서 놓친 VAT 이중계산 잔존 지점 발견·수정] "N월 계획출금 구성" 팝업(openPlanDetail — 대시보드의 "월별 계획 vs 실적" 표에서 출금 금액을 클릭하면 뜨는 실제 사용자 화면). 배경: 사용자가 "9월 계획출금을 확인해보라, 세금계산서 매입분이 아닌 급여나 사업소득(3.3%)의 경우에도 부가세를 더하고 있는 것 같다"고 지적, 이후 9월·10월 실제 화면 스크린샷 3장(8·9·10월)을 첨부하고 "부산은행 매입 숫자를 비교해보라"고 정확히 요청. 필자가 직접 재구성한 계산 값(9월 10,097,234원·10월 7,917,208원)과 사용자가 캡처한 실제 화면 값(9월 11,106,957원·10월 8,708,929원)을 정확히 대조한 결과 정확히 1.1배 차이임을 확인 — 계산서없음 매입(급여형·사업소득형, 이미 VAT 없는 순수 금액)에 부가세 10%가 잘못 더해지고 있었다. 근본 원인: v2.9.43에서 "NOINV- 접두사면 VAT를 곱하지 않는다"는 결함 패턴을 grep으로 찾아 4곳(openDetailModal의 표시·검산, renderPlanAct, 추정 재계산 로직)에서 고쳤다고 판단했으나, 실제 사용자가 대시보드에서 매번 클릭하는 이 팝업(openPlanDetail)은 완전히 별개의 함수라 검색에 걸리지 않아 결함이 그대로 남아 있었다 — v2.9.43·v2.9.46, 그리고 이번 세션 초반의 "9월/10월 검증" 모두 이 팝업을 직접 열어보지 않고 다른 모달(openDetailModal, "추정 잔액 구성")만 검증해 결함을 놓쳤다. 수정: openPlanDetail의 계획출금 계산에도 cost_id의 NOINV- 접두사를 확인해 계산서없음 항목은 VAT 없이, 세금계산서형만 ×1.1을 적용하도록 정정, 계산서없음 항목에 "(계산서없음)" 라벨 추가해 화면에서도 구분되도록 함. 화면에 존재하는 4개 모달(openDetailModal·openUnclassModal·openPlanDetail·renderTx)과 대시보드 배지 전체를 하나씩 직접 호출해 재검증 — 나머지는 이미 정확하거나(openDetailModal) 애초에 VAT 계산과 무관함(openUnclassModal·renderTx·대시보드 배지는 실적 데이터만 다룸)을 확인. 검증: 사용자가 제시한 실제 값과 정확히 일치(9월 10,097,234원, 10월 7,917,208원). 회귀 테스트 신설(e2e_openplandetail_vat.js, 3건). PRINCIPLES.md에 원칙 37 추가("결함 패턴을 고쳤다"는 판단은 그 패턴이 나타나는 화면을 전부 직접 열어본 뒤에만 성립 — grep으로 코드 패턴을 찾아 고치는 것과 그 결함이 나타나는 모든 화면을 확인하는 것은 다르다). 전체 스위트 158/158 통과.
[v2.9.46~47] 2026-09-16 — ①계산서없음 매입 지급일 규칙 확정 ②CSV 거래 내역 팝업에 재지정 드롭다운 추가.

[v2.9.46] 계산서없음 매입(급여형·사업소득형)의 지급일 규칙을 실제 운영 조건에 맞게 정정. 배경: 사용자 요청으로 "8월 계획출금을 현재 데이터 기준으로 직접 만들어보라"는 검증을 진행하던 중, 부산은행(PRJ-2026-007) 프로젝트의 8월 선지급 용역비(9,369,234원)가 8월 계획출금에 전혀 반영되지 않는 것을 발견 — v2.9.36에서 계산서없음 매입의 지급일을 "귀속월의 익월말"로 하드코딩해두었는데, 실제 부산은행 프로젝트는 "매출보다 매입이 선행되는 계약"(당월 근무분을 조기 지급)이라 이 가정과 맞지 않았다. 사용자와 논의를 통해 정확한 규칙을 확정: "세금계산서 매출·매입은 수금일·지급일이 이미 명시되어 있어 논의 불필요, 세금계산서가 아닌 매입분(급여·용역비 3.3%)만 지급 시기를 정하면 된다"로 범위를 좁히고, "회사 급여일이 매월 10일이며 전월 근무 이력을 당월 10일에 지급한다"는 실제 운영 규칙에 따라 계산서없음 매입의 지급일을 "귀속월의 익월 10일"로 확정(연도 경계 포함 정확히 처리 — 12월 근무분은 다음해 1월 10일). 검증: 실제 견적 데이터(김기황 시리즈 5개 항목 + 부산숙소 기타경비 2건)로 8월 계획출금을 처음부터 재구성해 기존 시트 값(39,433,448원)과 정확히 일치함을 확인, 8월 선지급분이 정확히 9월 10일 지급으로 재배치됨을 확인. 회귀 테스트에 정확한 날짜(연·월·일)를 직접 검증하는 케이스 추가(이전 테스트는 월만 확인해 이번 규칙 변경을 놓칠 뻔했음).

[v2.9.47] "CSV 거래 내역" 팝업(자금수지의 통장 대사 카드에서 여는 버튼)에 재지정 드롭다운 신설. 배경: 기존 "미분류·기타 재지정" UI는 카테고리가 없거나 "기타비용/기타입금"으로 분류된 거래만 대상으로 하여, 이미 다른 유형(예: 실수로 "법인카드"로 분류된 매입지급 건)으로 분류된 거래는 재지정할 방법이 없었다. 사용자 확정: 기존 미분류 UI는 그대로 유지, "CSV 거래 내역" 버튼을 눌렀을 때 나오는 팝업(건별 내역)에 별도로 재지정 드롭다운 추가. 구현: 팝업의 "유형" 컬럼을 텍스트 대신 코드 관리 등록 유형 전체를 보여주는 드롭다운으로 교체, id 접두사를 미분류 UI(rc_)와 분리된 rct_로 지정해 두 UI가 동시에 렌더링되어도 값이 서로 덮어쓰이지 않도록 함, saveReclass()가 두 접두사를 모두 처리하도록 확장, 저장 후 팝업이 열려 있으면 자동으로 다시 그려 드롭다운이 최신 상태를 반영하도록 함. 검증: 이미 "법인카드"로 분류된 거래를 CSV 팝업에서 "매입지급"으로 재지정·저장·재조회까지 정확히 동작함을 확인, 미분류 목록과 CSV 팝업이 동시에 열려 있을 때 각각의 재지정이 서로 섞이지 않고 정확히 저장됨을 확인.

회귀 테스트 확장(e2e_cashflow_noinvoice.js에 2건, 신규 파일 e2e_csv_reclass_popup.js 7건). PRINCIPLES.md에 원칙 35(하드코딩한 날짜 규칙은 그럴듯한 기본값이 아니라 실제 계약·운영 조건에서 가져올 것) · 원칙 36(사용자가 정확히 지목한 화면에 기능을 추가할 것 — 대안을 먼저 제시하지 말 것) 추가. 전체 스위트 155/155 통과.
[v2.9.45] 2026-09-16 — [근본 재설계] v2.9.44 편법(공백 제거)을 폐기하고 거래유형 집계를 원칙에 맞게 재설계. 사용자 지적 두 가지 인정: ①"띄어쓰기가 문제인게 아니고, 등록된 유형을 기준으로 분류하도록 해야지. 무조건 편법이라도 기능이 돌아가는 것처럼 보이게 하면 신뢰를 받을 수 없어" — 정확한 지적. v2.9.44는 "매출입금"과 "매출 입금"의 공백을 지워 우연히 일치시킨 미봉책이었을 뿐, 사용자가 실제 등록한 유형(보증보험·수수료·경조사비 등 이름도 개수도 하드코딩 9개와 전혀 다름)에는 애초에 통하지 않는 해법이었다. ②"CSV 대사 팝업에는 드롭다운으로 수정하는 기능이 없다" — 이전 답변에서 확인 없이 잘못 말한 것을 인정, 실제로 그 팝업(건별내역/유형별집계)은 순수 조회 전용이며 재지정 UI는 별도 위치(통장 대사 카드의 미분류 거래 섹션)에만 있음을 재확인. 근본 재설계: 코드 관리 화면(①거래유형&키워드)에 "집계 버킷" 지정 드롭다운을 신설 — 사용자가 유형을 등록할 때 그 유형이 자금수지의 어느 집계 카테고리(매출입금/매입지급/고정비/세금/법인카드/AI사용료/자본납입/기타입금/기타비용)에 속하는지 명시적으로 선택하고, 이 매핑을 S10 시트의 description 컬럼(그동안 이 카테고리들에서 항상 빈 문자열로만 쓰이던 컬럼, 스키마 변경 없음)에 저장. 자금수지(cashflow.html)의 두 집계 함수(화면 재진입 재집계·CSV 파싱 직후 실시간 집계)는 더 이상 하드코딩 문자열과 비교하지 않고 bucketOf() 함수로 이 매핑을 조회해 addToBucket()으로 집계 — 유형명이 무엇이든, 철자가 어떻든, 몇 개를 등록하든 항상 정확하다. 버킷을 지정하지 않은 유형(구버전 데이터 포함)은 안전하게 기타 항목으로 폴백해 누락되지 않음. 검증: "매출 입금"·"매입 지급"·"보증보험"(하드코딩 9개에 전혀 없는 이름)을 각각 매출입금·매입지급·기타비용 버킷으로 지정해 정확히 집계됨을 확인, 코드 관리 화면에서 저장한 버킷이 재조회 시 드롭다운 선택 상태로 정확히 복원됨을 확인, 버킷 미지정 유형도 누락 없이 기타로 폴백됨을 확인. 회귀 테스트 전면 재작성(e2e_cashflow_whitespace.js, v2.9.44 전제의 무효한 케이스를 근본 설계 기준 7건으로 교체). PRINCIPLES.md에 원칙 34 추가(원칙33 정정 — 증상을 없애는 변형이 아니라 구조 자체를 사용자가 통제하게 할 것: 사용자 자유 입력을 하드코딩 문자열로 다시 알아내려는 구조를 발견하면 문자열 비교를 정교화하지 말고 등록 시점에 명시적 매핑을 받도록 구조를 바꿀 것). 전체 스위트 146/146 통과.

※ 사용법 안내: 설정 화면의 코드 관리 ①거래유형&키워드에서 기존에 등록해두신 유형들(매출 입금·매입 지급·세금 납부·자본 납입·보증보험·수수료·경조사비 등)에 이제 "집계 버킷" 선택 드롭다운이 추가로 보입니다. 각 유형에 맞는 버킷을 선택해 저장해 주셔야 자금수지에 정확히 집계됩니다(예: "매출 입금"→매출입금, "보증보험"→기타비용). 이미 등록하신 유형들은 버킷 미지정 상태이므로, 한 번 열어서 버킷을 지정해 주시기 바랍니다.
[v2.9.44] 2026-09-16 — [최종 원인 확정] 자금수지 값 왜곡 사고의 진짜 근본 원인 발견·수정: 사용자 정의 거래유형명의 공백 불일치. 배경: v2.9.42(필드명 불일치)·v2.9.43(VAT 이중계산)를 고쳤음에도 사용자가 "바뀐 게 없다"고 재확인해, 사용자 요청대로 Google Drive로 실제 운영 중인 S10_SETTINGS·S08_DETAIL 시트를 직접 읽어 원인을 확정했다. 실제 시트 확인 결과: 사용자가 코드 관리 화면에서 "매출 입금"·"매입 지급"·"세금 납부"·"자본 납입"처럼 공백이 포함된 유형명으로 등록해두었고, classify() 함수는 이 이름을 정확히 그대로("매출 입금" 등) 반환해 S08_DETAIL에 정확히 분류·저장되어 있었다. 그런데 aggregateMonthly()의 두 switch문(화면 재진입 시 재집계용, CSV 파싱 직후 실시간 집계용)이 "매출입금"·"매입지급"·"세금"(공백 없음) 하드코딩 문자열만 인식하도록 되어 있어 — "매출 입금" ≠ "매출입금"이라 전혀 매칭되지 않고, default 분기도 없어 개별 카테고리(매출입금·매입지급·고정비·세금)가 전부 0으로 집계되는 동안 총계(act_inflow/act_outflow)에는 반영되는 기이한 패턴으로 나타났다 — 이것이 스크린샷에서 "매출 입금 0원인데 총 출금은 큰 값"으로 보였던 정확한 원인. 수정: 두 switch문 모두 비교 직전 공백을 제거하는 정규화(catN=String(t.category||'').replace(/\s/g,''))를 적용, "세금납부" case도 추가, 두 번째 switch문에는 원래 없던 default 분기를 신설해 9개 고정 카테고리 밖의 완전히 새로운 유형명(자본 납입·수수료·경조사비·보증보험 등)도 최소한 기타 항목으로는 누락 없이 집계되도록 방어. 검증: 실제 시트와 동일한 공백 포함 유형명("매출 입금" 등)으로 재현해 개별 카테고리가 정확히 집계됨을 확인, 화면 재진입 재집계와 CSV 파싱 직후 실시간 집계 양쪽 경로 모두 확인. 답변(잘못된 매칭 수정 방법): 자금수지 화면의 "통장 대사" 카드에서 각 미분류 거래 옆 드롭다운으로 재지정 가능(v2.9.42에서 이 드롭다운의 필드명 결함을 수정함) — 다만 이번 사고는 재지정이 필요한 게 아니라 이미 정확히 분류된 데이터를 집계 로직이 못 알아보는 문제였으므로, 이 수정 이후에는 별도 재지정 없이 정상 집계된다. 회귀 테스트 신설(e2e_cashflow_whitespace.js, 7건). PRINCIPLES.md에 원칙 33 추가(사용자 자유 입력 문자열과 하드코딩 문자열을 비교하는 곳은 반드시 정규화할 것 — 그리고 화면 증상만으로 반복 추정하지 말고 연결된 도구로 원본 데이터를 직접 읽을 것, 원칙27·29와 짝을 이루는 최우선 계열). 전체 스위트 146/146 통과.
[v2.9.43] 2026-09-16 — 자금수지 계획출금 값 왜곡 및 상세 모달 매입/고정비 구분불가 결함 수정(사용자 지적: "월별 추정 상세 내역이 이해가 안 되고, 고정비 수정 결과를 여전히 확인 못하겠다"). 두 가지 결함을 발견해 수정: ①상세 모달에서 매입 항목이 "지급예정 PRJ-XXX"로만 표시되어 고정비("고정비 [항목명]")와 시각적으로 구분이 안 되던 문제 — 세금계산서형은 "매입", 계산서없음(v2.9.36)은 "매입(계산서없음)"으로 명확히 구분 표시하도록 수정. ②[핵심] planCost 배열에 세금계산서형 매입(단가만 있고 VAT 별도 계산 필요)과 계산서없음 매입(v2.9.36에서 견적 기준 직접계산, 이미 VAT 없는 순수금액)이 함께 담겨 있는데, 이 배열을 소비하는 "월별 계획 vs 실적" 표의 계획출금 계산·상세 모달의 검산 소계·추정 재계산(recalcEstimate) 로직 등 최소 4곳이 전부 계산서없음 항목에도 무조건 VAT 10%(×1.1)를 곱하고 있어 자금수지 계획출금 자체가 실제보다 부풀려지고 있었음 — cost_id가 "NOINV-"로 시작하는 항목은 이미 VAT 포함 순수금액이므로 곱하지 않도록 4곳 모두 수정. 검증: 세금계산서형(7,000,000원)+계산서없음(8,000,000원) 혼재 시나리오에서 계획출금이 정확히 15,700,000원(7,700,000+8,000,000, 이중계산 없음)으로 계산됨을 확인, 상세 모달에서 두 유형이 명확히 구분 표시됨을 확인. 부가로 v2.9.42에서 txnTypeNames()(미분류 거래 재지정 드롭다운)의 필드명 불일치(원칙30 계열, r.key/r.value→setting_key/setting_value)도 발견해 함께 수정 — 코드 관리에서 등록한 거래유형이 이 드롭다운에 정확히 반영되지 않던 것. 회귀 테스트 확장(e2e_cashflow_noinvoice.js에 3건 추가), PRINCIPLES.md에 원칙 32 추가(하나의 배열에 성격이 다른 항목을 섞어 넣었다면 그 배열을 소비하는 모든 계산식을 전수 확인할 것 — 놓친 지점은 에러 없이 조용히 잘못된 숫자를 만들어냄). 전체 스위트 139/139 통과.

※ 이번 수정으로도 자금수지가 여전히 이상하게 나온다면, 다른 원인이 섞여 있을 수 있습니다 — 실제 시트 데이터(CSV export나 스크린샷)를 보여주시면 더 정확히 진단하겠습니다.
[v2.9.42] 2026-09-16 — 미분류 거래 재지정 드롭다운의 필드명 불일치 결함 발견·수정(사용자 질문: "미분류 거래 지정은 어디서 항목을 읽어오는지, 수정된 내용과 부합하는지" — 자금수지를 처음부터 재작업해야 할 정도로 값이 엉망이 된 상황에서 나온 질문으로 실제 원인 발견에 기여). txnTypeNames() 함수가 classify()·fixed.html의 항목 자동완성과 완전히 동일한 필드명 불일치(옛 r.key/r.value 참조, S10은 setting_key/setting_value로 통일된 지 오래됨)를 갖고 있어, 미분류·기타 거래 재지정 드롭다운이 코드 관리 화면(①거래유형&키워드)에서 등록한 유형명을 전혀 못 읽고 항상 하드코딩된 9개 기본값(매출입금·매입지급·고정비·급여·세금·법인카드·AI사용료·기타비용·기타입금)만 보여주고 있었음 — v2.9.40에서 classify()와 fixed.html은 고쳤으나 이 함수를 놓쳤던 것. setting_key/setting_value로 정정, settings.html의 유사한 옛 필드명 폴백(loadCodes 내 kw() 헬퍼)도 함께 수정. 검증: 코드 관리에서 "세금납부"·"통신비지출" 같은 커스텀 유형을 등록하면 재지정 드롭다운에 정확히 나타남을 확인. 답변: ②고정비 대사는 자금수지 화면의 "통장 대사" 카드에서 확인 — CSV 업로드 시 "미매칭 출금(고정비 분류)" 섹션이 매입지급 섹션 바로 아래 나타나며, 매칭 완료 건은 고정비 계획 화면(fixed.html)에서도 "지급완료" 배지로 확인 가능(v2.9.41에서 구현). 회귀 테스트 확장(e2e_data_loss_guard.js에 1건 추가), 전체 스위트 136/136 통과.

※ 자금수지 값이 엉망이 되어 재작업이 필요하다고 하신 부분: 이번에 고친 결함(재지정 드롭다운이 원하는 유형을 못 보여줘 잘못 지정했거나 지정 자체를 포기했을 가능성)이 원인 중 하나일 수 있습니다. 전체를 처음부터 다시 돌리시기 전에, 이 수정판으로 먼저 미분류 거래 재지정을 다시 시도해 보시길 권해드립니다.
[v2.9.41] 2026-09-15 — 고정비 통장 대사 기능 신설(사용자 확정: "고정비 계획을 입력하고 있으니, 자금수지에서 대사 결과를 보여줄 방법을 모색해보자" → 매출·매입과 동일한 자금수지 통장 대사 화면에 통합하는 방향으로 확정). 배경: 매출(S04)·매입(S05)에는 통장 CSV와 계획을 자동/수동으로 매칭해 "실제로 입금·지급됐는지" 확인하는 대사 기능(matched_txn_id, 지급완료 배지, 대사 해제)이 완비되어 있었는데, 고정비(S06)만 이 개념 자체가 스키마부터 빠져 있었음 — "계획"만 있고 "실제 지급 확인"이 없는 상태로 방치. 구현: S06_FIXED_COST 스키마에 matched_txn_id·pay_actual_date 2개 컬럼 추가(A:H→A:J, js/schema.js R·W·HEADERS 갱신). 자금수지(cashflow.html)의 통장 대사 엔진에 고정비 대사를 매입 지급 대사와 완전히 대칭되는 방식으로 추가 — reconcile()에서 통장 CSV의 "고정비" 분류 출금을 미대사 고정비 계획과 금액(세금계산서 수취 항목은 amount+vat_amt, 미수취는 amount 그대로)·날짜(±45일)로 자동 매칭, 후보가 여러 건이면 fixCands()로 후보 제시 후 manualMatchFix()로 수동 매칭, rebuildRecon()이 재진입 시 미매칭 고정비 출금도 정확히 재구성, 통장 대사 카드에 "미매칭 출금(고정비 분류)" 섹션 신설. 고정비 화면(fixed.html)에는 대사 완료 건에 "지급완료" 배지와 실지급일 표시, 잘못 매칭된 경우 "지급 해제"로 대사 정보만 제거하고 계획(항목명·금액)은 보존하는 기능 추가. 구현 중 발견·수정한 위험: fixed.html의 저장 함수(writeFx)가 예전 8컬럼 헤더를 하드코딩하고 있어, 그대로 뒀다면 고정비를 수정할 때마다 새로 추가한 대사 정보(matched_txn_id·pay_actual_date)가 조용히 사라지는 v2.9.37/38과 동일 유형의 사고로 이어질 뻔했음 — js/schema.js의 HEADERS.S06(SSOT)을 참조하도록 정정. 검증: 통장 CSV의 임차료 자동이체 거래가 고정비 계획과 자동으로 정확히 매칭됨을 확인, 고정비 화면에서 지급완료 배지·지급 해제가 정확히 동작하고 계획 자체는 보존됨을 확인, 다른 고정비 건을 수정해도 이미 대사된 건의 정보가 함께 지워지지 않음을 확인. 신규 회귀 테스트 파일(e2e_fixed_recon.js, 9건) 추가, PRINCIPLES.md에 원칙 31 추가(같은 성격의 항목군(매출·매입·고정비)은 기능도 서로 대칭인지 주기적으로 점검할 것 — 한 항목에만 만든 기능은 다른 항목에 조용히 누락된 채 남기 쉬움). 전체 스위트 135/135 통과.
[v2.9.40] 2026-09-15 — 코드 관리(①거래유형&키워드 ②거래점 분류규칙 ③고정비 항목 ④기술태그) 사양 확정 및 전수검사 결과 발견한 핵심 결함 수정. 사용자 확정: v2.9.39에서 복원한 "CSV 자동분류 키워드"·"거래점 분류 규칙" 두 카드는 삭제가 실수가 아니라 의도적 결정이었음(현재 코드 관리의 세 가지 입력 패턴만으로 충분) — 두 카드를 재제거. 사용자가 정의한 사양: ①거래유형&키워드에 "매출입금/매입지급/세금납부/자본납입" 등을 자유롭게 등록하되 그중 "법인카드"류는 ②거래점분류규칙과, "고정비"류는 ③고정비항목과 연동되어야 함 ②거래점분류규칙의 두 번째 필드는 ①에서 등록한 유형명을 동적 콤보리스트로 제공해야 함 ③고정비항목은 고정비 화면의 항목·적요 정의용 ④기술태그는 인력프로필 기능 개발 중 추가된 것으로 검수 대상 제외하되 UI 버그(고정비 "+항목 추가" 버튼이 기술태그 섹션에 위치)만 수정. 전수검사 결과 발견한 핵심 결함: cashflow.html의 classify() 함수와 fixed.html의 항목 자동완성 로직이 S10 시트를 여전히 옛 필드명(r.key/r.value)으로 참조하고 있었는데, S10은 v2.9.9부터 setting_key/setting_value로 헤더가 통일되어 있어 이 참조가 항상 undefined만 반환 — 코드 관리 화면에서 아무리 거래유형·거래점규칙·고정비항목을 등록해도 자금수지 자동분류와 고정비 화면 자동완성에 전혀 반영되지 않는 심각한 결함이었다(에러 없이 조용히 무력화되어 화면만 봐서는 발견 불가능). classify()와 fixed.html의 필드 참조를 setting_key/setting_value로 정정. UI 버그 수정: "+ 항목 추가"(고정비) 버튼이 ④기술태그 섹션(ctSkill) 아래에 위치해 기술태그 버튼처럼 보이던 것을 ③고정비 섹션(ctFixed) 바로 뒤로 이동. 검증: 코드 관리에서 "세금납부"(원천세,법인세 키워드) 유형을 등록하면 통장 거래 자동분류에서 정확히 "세금납부"로 판정됨을 확인(수정 전에는 하드코딩된 "세금"으로만 분류됨), "하나은행0026→법인카드" 거래점 규칙도 정확히 반영됨을 확인, "소프트웨어구독료" 고정비 항목이 고정비 화면 자동완성 목록에 정확히 나타남을 확인, 버튼 위치가 정확한 섹션 아래로 이동됨을 확인. 회귀 테스트 재정비(e2e_data_loss_guard.js의 무효화된 v2.9.39 케이스를 v2.9.40 기준 6건으로 교체), PRINCIPLES.md에 원칙 30 추가(여러 화면이 같은 시트를 읽을 때 필드명을 반드시 통일하고 grep으로 확인할 것 — 필드명 불일치는 에러 없이 조용히 무력화되어 직접 재현 전에는 발견 불가능). 전체 스위트 126/126 통과.

※ ③고정비 항목의 "대사 키워드(선택)" 필드는 저장·로드는 되지만 이를 실제로 소비하는 로직(대사·자동매칭)이 어느 화면에도 없음을 전수검사로 확인했습니다. 입력은 받아도 아무 데도 쓰이지 않는 상태이니, 이 필드에 별도 용도를 부여하실 계획이 있으시면 말씀해 주시기 바랍니다.
[v2.9.39] 2026-09-15 — CSV 자동분류 키워드·거래점 분류 규칙 입력 UI 복원(사용자 지적: "이 UI가 필요한지는 실제 소스에서 어떻게 쓰는지 찾아봐야지, 내가 결정할 수 있나?" — 판단을 사용자에게 미루지 말고 소스에서 사실관계를 먼저 확인하라는 정확한 지적). 조사 결과: cashflow.html의 통장 CSV 자동분류 로직(classify)이 지금도 kw_revenue_inflow·kw_cost_outflow·kw_fixed_outflow·kw_tax_outflow·kw_capital_inflow·kw_card_branch·kw_ai_branch 값을 S10에서 읽어 실제로 사용하고 있었으나(카테고리 구분 없이 시트 전체를 읽어 settings 객체에 채우는 구조), 이 값을 입력할 <input> 요소 자체가 예전 리팩터링 중 삭제된 채 "<!-- CSV 키워드 -->", "<!-- 거래점 분류 규칙 -->" 주석 흔적만 남기고 방치되어 있었음 — 기능은 살아있는데 사용자가 값을 바꿀 창구가 없는 명백한 UI 누락 버그. 복원: "CSV 자동분류 키워드" 카드(매출입금/매입지급/고정비/세금/자본납입 키워드 5개 입력칸, 기존 saveKeywords() 버튼과 연결)와 "거래점 분류 규칙" 카드(법인카드/AI사용료 거래점 패턴 2개 입력칸, 기존 saveBranchRules() 버튼과 연결)를 원래 있어야 할 위치에 재구성 — 저장·로드 로직(loadSettings, saveSettingsToSheet)은 이미 v2.9.38에서 코드 관리 데이터와 안전하게 공존하도록 고쳐져 있어 그대로 재사용. 검증: 복원된 입력칸에 기존 저장값이 정확히 로드됨을 확인, 새 키워드 입력 후 저장 시 정확히 반영되고 기존 코드 관리 데이터(원칙28)도 함께 보존됨을 확인, 설정 화면에서 저장한 키워드가 자금수지(cashflow.html)의 CSV 자동분류 로직에 실제로 반영되는 종단간 흐름까지 확인. 회귀 테스트 확장(e2e_data_loss_guard.js에 5건 추가, 총 14건), PRINCIPLES.md에 원칙 29 추가("이 UI가 필요한가"는 사용자에게 묻지 말고 소스에서 실제 사용 여부를 확인해 스스로 판단할 것 — 다른 화면이 그 값을 실제로 쓰고 있다면 판단이 아니라 조치가 필요한 사안). 전체 스위트 125/125 통과.
[v2.9.38] 2026-09-15 — 설정 화면(S10_SETTINGS 시트)의 소스 전수 검사(사용자 요청) 결과 발견한 데이터 오염·삭제 결함 2건 수정. 배경: v2.9.37에서 markScheduleChanged의 "읽기 실패 시 덮어쓰기" 사고를 고쳤으나, 사용자가 첨부한 실제 시트 스크린샷(setting_key·setting_value는 비어있고 description에만 값이 남은 이상한 행들)을 근거로 "이 시트에서 대체 어떤 작동을 하는지" 전수 검사를 요청받아 조사한 결과, v2.9.37과는 별개의 훨씬 직접적인 원인 2건을 추가로 발견. ①saveSettingsToSheet()(설정 화면의 "키워드 저장"·"분류 규칙 저장"·"파라미터 저장" 3개 버튼이 공통으로 호출)가 settingsMap(키워드·파라미터 항목만 담긴 객체)만 갖고 S10 시트 전체를 clearAndWrite — 코드 관리 화면(saveCodes)이 저장한 거래유형·거래점규칙·고정비항목·스킬태그(txn_type 등) 데이터를 전혀 모르는 채 시트를 통째로 덮어써, 이 3개 버튼 중 아무거나 누르면 그 순간 코드 관리 데이터가 사라졌음. ②loadSettings()가 카테고리 구분 없이 setting_key가 있는 모든 행(코드 관리 카테고리 포함)을 settingsMap에 담아, ①의 수정만으로는 부족 — settingsMap에 이미 섞여 들어간 코드 관리 데이터가 저장 시 keyword/parameter로 잘못 재분류되어 category 필드까지 오염되는 2차 결함이 있었음. 수정: loadSettings()가 코드 관리 카테고리 행을 settingsMap에서 원천 배제, saveSettingsToSheet()는 저장 직전 시트를 다시 읽어와 코드 관리 카테고리 행은 그대로 보존한 채(원칙27에 따라 그 읽기가 실패하면 쓰기 자체 포기) settingsMap 부분만 교체해 병합. 추가로 saveCodes()와 saveSettingsToSheet()가 S10에 쓰는 헤더 컬럼 순서(description/category 위치)가 서로 달랐던 것도 통일해 향후 같은 종류의 혼선을 예방, saveCodes()도 저장 직전 최신 상태를 다시 확인하도록 보강. 검증: "키워드 저장" 버튼(saveSettingsToSheet 경유)을 눌러도 기존 거래유형 코드가 category까지 정확히 보존됨을 확인, 코드 관리 화면에서 신규 유형 추가 후 저장해도 기존 키워드·파라미터가 보존됨을 확인(양방향 검증). 회귀 테스트 확장(e2e_data_loss_guard.js에 4건 추가, 총 9건), PRINCIPLES.md에 원칙 28 추가(한 시트를 여러 함수가 나눠 관리하면 "이 함수가 모르는 데이터"를 읽기·쓰기 양쪽에서 명시적으로 다룰 것). 전체 스위트 120/120 통과.

※ 이 버전을 받으신 후: 조사 과정에서 첨부해 주신 스크린샷의 오염된 행들(설명만 있고 키·값이 빈 행)은 이 수정으로 자동 정리되지 않습니다. 설정 화면을 여신 후 코드 관리·키워드·파라미터 값이 정확한지 한 번 확인해 주시고, 이상이 있으면 다시 입력 후 저장해 주시기 바랍니다.
[v2.9.37] 2026-09-15 — [최우선/치명적] 설정 화면(코드 관리)의 거래유형·키워드 데이터가 사용자 의지와 무관하게 전부 삭제되던 실제 데이터 유실 사고 발견·수정(사용자 신고: "8월 마감을 한 걸로 봐서는 데이터가 있었을 것이고 내 의지와 관계없이 지워진 걸로 추정"). 원인 규명: 청구 스케줄을 저장할 때마다 자동 실행되는 부가 기능 markScheduleChanged()(v2.9.20, "마지막 스케줄 변경 시각"을 S10에 기록해 자금수지 재계산 필요 배너를 띄우는 용도)가 S10 전체를 다시 읽어(fetchSheetSafe) 그 값 하나만 갱신해 병합한 뒤 clearAndWrite로 시트 전체를 새로 쓰는 구조였는데, 그 읽기가 네트워크 일시 오류 등 어떤 이유로든 실패해 null을 반환하면 toObj(rows||[])가 이를 "S10이 원래 비어있다"로 오인 — 사용자가 설정 화면에서 몇 달간 직접 쌓아온 거래유형·키워드 코드 전체가 last_schedule_change_at 항목 하나만 남긴 채 흔적도 없이 사라지는 사고로 이어졌다. 이 결함은 청구 스케줄을 정상적으로 계속 사용하는 동안은 전혀 드러나지 않다가, 어느 한 번의 읽기 실패 순간에만 터지는 가장 발견하기 어려운 유형이었다. 동일 패턴이 마일스톤 설정(S17, saveGen 내 milestones 배열 재사용)에도 존재해 함께 수정 — 페이지 로드 시점에 S17 읽기가 실패하면 milestonesLoadOk 플래그를 false로 세팅하고, 이 플래그가 false인 상태에서는 마일스톤 저장 자체를 건너뛰어 다른 프로젝트의 기존 마일스톤 설정이 유실되지 않도록 방어. 수정 원칙: "읽어서 병합 후 전체 쓰기" 패턴은 읽기가 명확히 실패했다고 판단되면(null 응답) 쓰기 자체를 포기해야 한다 — 부가 기능 하나가 이번에 갱신되지 않는 손실보다 사용자의 기존 데이터를 지키는 것이 항상 우선이다. 검증: S10·S17 읽기가 실패하는 상황을 직접 재현해 markScheduleChanged()와 마일스톤 저장이 쓰기 자체를 시도하지 않음을 확인, 정상 케이스(읽기 성공)에서는 기존 데이터 보존+새 값 정상 병합이 회귀 없이 유지됨을 확인. 신규 회귀 테스트 파일(e2e_data_loss_guard.js, 5건, 스위트 내 최우선 표시) 추가, 테스트 헬퍼(_e2e_helpers.js)에 fetchFailFor 옵션 신설(특정 시트의 읽기 실패를 시뮬레이션). PRINCIPLES.md에 원칙 27 추가 — "다른 모든 원칙보다 우선하는 최우선 원칙"으로 명시(UI가 틀리면 다시 고치면 되지만 사라진 데이터는 되돌릴 수 없음). 전체 스위트 116/116 통과.

※ 이 버전을 받으신 후: 이미 유실된 설정 화면(코드 관리)의 거래유형·키워드 데이터는 이 수정으로 복구되지 않습니다. 백업이 있다면 복원하시고, 없다면 다시 입력해 주셔야 합니다. 죄송합니다.
[v2.9.36] 2026-09-15 — "세금계산서" 메뉴의 정체성 재정정: 청구 스케줄 생성은 세금계산서형만, 계산서없음 매입은 자금수지가 견적에서 직접 계산(사용자 지적: 계산서 수취가 필요 없는 매입에 "공급가액"·"수취예정"·합계(VAT포함) 같은 세금계산서 용어를 붙이는 것 자체가 전제부터 틀렸음 — "심지어 부가세까지 표시하며 미친 결과를 낸다"는 강한 문제 제기). v2.9.27~35에서 "매입 스케줄(자금유출 추적)은 지급형태 무관 전원 포함, 부가세만 형태별로 구분"으로 설계했던 것을 전면 재정정. 확정 방향(사용자): ①청구 스케줄 생성 버튼은 세금계산서형만 만든다 ②계산서없음(사업소득형·급여형·미수취 기타경비)의 지급 예정은 자금수지 화면에서만 보이면 되고 별도 목록 관리 불필요 ③그 데이터는 청구 스케줄을 거치지 않고 자금수지가 견적(S12)에서 직접 월별 계산해 반영. 변경 내용: input.html — genCtx()의 eBuy를 세금계산서형(itemsInvoice)만으로 계산(eBuyInvoice/eBuyNonInvoice 변수 제거), buildGen·msCalc의 매입 생성 로직에서 계산서없음 분기 삭제, 안내 문구를 "계산서없음 매입은 자금수지에서 별도 반영됩니다"로 정정, renderLists()가 invoice_plan_date/invoice_date 유무로 세금계산서형만 목록에 표시(계산서없음 완전 제외). cashflow.html — loadSheets()에 견적(S12)의 계산서없음 항목을 프로젝트별·월별로 monthlyProfile 재사용해 직접 분배 계산하는 로직 신설, planCost에 세금계산서형(S05 원본)과 계산서없음(견적 직접계산) 결과를 병합(중복 없이). estimate.html — 사업소득형·급여형·기타원가 미수취 체크박스의 안내 문구를 "계산서가 없으므로 세금계산서 화면에는 나타나지 않고 자금수지에 지급 예정으로 자동 반영됩니다"로 정정. 검증: 3인 혼합 견적(세금계산서형 외주+계산서없음 사업소득형+계산서없음 급여형)에서 청구 스케줄은 세금계산서형(49,000,000원)만 정확히 생성, 자금수지는 세금계산서형(S05)+계산서없음(견적계산)을 이중계산 없이 정확히 합산(15,000,000원 등 다수 시나리오), 견적이 전부 세금계산서형이면 계산서없음 항목이 과다생성되지 않음을 확인. 회귀 테스트의 v2.9.27~31 관련 3개 파일(e2e_schedule, e2e_etc_noinvoice, e2e_ui_symmetry)을 새 설계에 맞춰 전면 갱신, 신규 파일(e2e_cashflow_noinvoice.js, 8건) 추가. PRINCIPLES.md에 원칙 26 추가(화면의 정체성을 먼저 정의하고 그 정체성에 안 맞는 데이터를 욱여넣지 말 것 — 관련은 있지만 정체성이 다른 데이터는 필터로 숨기지 말고 별도 경로를 만들 것). tests/README.md에 v2.9.24→27→36으로 두 번 뒤집힌 설계 이력을 명시(향후 과거 커밋 메시지를 오독하지 않도록). 전체 스위트 111/111 통과.
[v2.9.35] 2026-09-14 — 기타원가(인프라·솔루션·기타) 세금계산서 수취 여부 구분 신설 + 매출·매입 개별 입력 폼 완전 삭제(사용자 지적: 원격지 근무 기타경비 728,000원이 세금계산서 발행 내역이 없었는데도 세금계산서형으로 잘못 분류되어 마일스톤 회차(계약금/중도금/잔금)로 무의미하게 쪼개짐). 원인: 인건비만 지급형태(세금계산서형/사업소득형/급여형) 구분이 있고 기타원가는 구분 자체가 없어 무조건 세금계산서형으로 취급되던 설계 공백. A) 지급형태 구분: 처음 제안한 "급여형·사업소득형" 체계는 근로 대가에만 성립하는 개념이라 기타경비(물품·인프라·서비스 매입)에는 근거가 성립하지 않음(사용자 지적으로 인정) — "세금계산서 수취 여부" 단일 축으로 재설계, 견적서 인프라·솔루션·기타 항목에 체크박스 추가(기본값=수취, 기존 데이터와 완전 호환), memo에 "_NOINVOICE_" 접두사로 스키마 변경 없이 인코딩(원래 비고 텍스트는 그대로 보존), input.html의 isInvoiceType 필터를 인건비뿐 아니라 기타원가까지 확장 적용. B) 개별 입력 폼 삭제: 예외 처리를 위한 매출·매입 수동 등록 폼(및 목록 카드의 "수정" 버튼)을 완전히 제거 — 예외 상황은 견적을 정확히 재작성한 뒤 청구 스케줄을 재생성하는 하나의 경로로 통일(사용자 결정). 작업 중 발견·수정한 2차 사고: ①폼 삭제 범위에 다른 여러 함수가 공유하던 시트 헤더 상수(H4·H5)가 함께 삭제되어 청구 스케줄 생성·발행완료 처리 등이 전부 깨질 뻔함 ②loadAll()이 삭제된 폼의 프로젝트 드롭다운을 초기화하던 코드를 그대로 남겨둬 null 참조 예외로 화면 전체가 "로드 실패"로 멈추던 결함 — 둘 다 회귀 테스트로 발견·즉시 수정. 검증: 728,000원 기타경비가 체크 해제 시 "계산서없음"으로 정확히 분류되어 청구 스케줄에서 세금계산서형 집계(eBuyInvoice)에서 제외됨을 확인, 견적서 체크박스의 저장·재조회 정확성 확인, 폼 삭제 후에도 매출·매입 목록이 정상 렌더됨을 확인. 회귀 테스트 신설(e2e_etc_noinvoice.js, 10건), 기존 e2e_ui_symmetry.js의 무효화된 폼 관련 케이스 정리. PRINCIPLES.md에 원칙 24(한 맥락의 분류체계를 다른 맥락에 그대로 끌어쓰지 말 것)·25(삭제 작업 시 참조 무결성 전수 확인) 추가. 전체 스위트 104/104 통과.
[v2.9.34] 2026-09-14 — 세금계산서 매입 수취 목록 카드의 금액 표시 결함 수정(사용자 지적: 매출·매입 표기 항목이 다른 걸 더 찾아보라). 매출 카드는 li-meta에 "공급가액+VAT" 세부내역을, li-amt에 "합계"(VAT포함) 라벨과 지급/입금 예정일을 함께 보여주는데, 매입 카드는 이 구조 자체가 없어 c.amount(공급가액, VAT 제외)를 아무 라벨 없이 그대로 노출하고 있었음 — 화면만 보고는 그 숫자가 공급가액인지 실제 지급 총액인지 구분할 수 없어 실무자가 지급액을 착각할 수 있는 심각한 표시 오류였음(단순 UI 미관 차이가 아님). 매출과 동일하게 li-meta에 "공급 OOO원 + VAT OOO원"(계산서없음 건은 "계산서없음"으로 명확히 구분), li-amt에 "합계 OOO원(VAT포함) (지급예정 날짜)"를 추가. 검증: 스크린샷 실제 수치(공급 2,623,000원, VAT 262,300원)로 합계가 정확히 2,885,300원(실제 지급 총액)으로 표시됨을 확인, 계산서없음(vat_amt=0) 건은 합계=공급가액 그대로 정확히 표시되고 "계산서없음"이 명확히 표기됨을 확인. 회귀 테스트 추가(e2e_ui_symmetry.js에 5건), PRINCIPLES.md에 원칙 23 추가(목록 화면의 금액은 어떤 금액인지 반드시 라벨로 밝힐 것 — VAT 포함/제외는 특히). 전체 스위트 103/103 통과.
[v2.9.33] 2026-09-14 — 세금계산서 화면의 매출 발행·매입 수취 탭 UI 정밀 대칭 점검(사용자 요청: 더 찾아보라). 확정 처리 3건: ① 매출 폼의 revTitle(수정 모드 표시 의도였으나 실제 갱신 로직이 없던 죽은 코드) 제거 ② 매입 VAT 박스에 매출과 동일한 안내 문구("합계 금액이 자금수지 비교 기준(지급 예정액)입니다") 추가 ③ 매출·매입 폼 공통으로 "취소"·"초기화" 버튼이 둘 다 reset*()만 호출해 완전히 중복이던 결함 수정 — closeForm() 신설로 "취소"는 폼을 접고 비움, "초기화"는 펼친 채로 내용만 비움으로 역할 분리. 추가로 발견한 핵심 비대칭(④): 매출의 markInvoiced()는 발행 완료 처리 시 입금예정일이 비어있으면 자동 계산해 채우는데, 매입의 markCostInvoiced()는 겉보기엔 완전히 대칭인 "수취 완료" 버튼이면서도 지급예정일을 전혀 건드리지 않아 방치되고 있었음 — 텍스트·레이아웃은 이미 대칭이라 겉모습만 봐서는 드러나지 않고 함수 본문을 나란히 비교해야 발견되는 결함이었음. 매출과 동일하게 지급예정일이 비어있으면 익월말로 자동 계산해 채우도록 수정. 검증: 지급예정일 공란인 매입 건에 수취완료 처리 시 자동으로 익월말 채워짐 확인, 취소 버튼이 폼을 접고 내용을 비우는 것 확인. 회귀 테스트 추가(e2e_ui_symmetry.js에 1건), PRINCIPLES.md에 원칙 22 추가(대칭 점검은 텍스트·레이아웃에서 멈추지 말고 같은 이벤트가 양쪽에서 같은 일을 하는지까지 볼 것). 전체 스위트 98/98 통과.
[v2.9.32] 2026-09-14 — 세금계산서 화면(input.html)의 매출·매입 UI 대칭 표준 적용(사용자 지적: 같은 UI 표준 기준으로 매출·매입 청구를 살펴보라 — 매입은 유형·항목명·단가×MM 계산기를 갖췄는데 매출은 공급가액 직접입력만 가능했고, 청구 스케줄 미리보기 표도 매입에만 "항목" 컬럼이 있어 컬럼 수 자체가 달랐음). 개별 등록(예외용) 폼: 매출에도 유형(인건비/외주비/기타원가)·항목명·단가(원/MM)·MM 필드와 자동계산기(calcRevAmt)를 추가해 매입 폼과 완전히 대칭되는 구조로 재구성. S04 시트에는 매입(S05)과 달리 cost_type/person_name/unit_price/mm 컬럼이 없어 스키마 변경 대신 memo 필드에 "유형|항목명|단가|MM|메모"로 구분자 인코딩(encodeRevMemo/decodeRevMemo), 기존 순수 텍스트 memo 데이터는 파싱 실패 시 원문 그대로 보존해 하위호환 유지. 목록 카드도 매입과 동일하게 유형·항목명·MM을 표시하도록 개선. 청구 스케줄 미리보기 표: 매출 표에도 "항목" 컬럼 추가(genRows 각 행에 name 부여 — 인력공급 방식은 월별 합산명, 마일스톤 방식은 계약금/중도금/잔금), 매출·매입 표가 동일한 5컬럼 구조로 통일. 검증: 매출 개별등록 폼의 단가×MM 자동계산(8,500,000×2=17,000,000) 및 인코딩 저장 정확, 목록 카드·수정 폼 복원 정확, 기존 텍스트 memo 하위호환 확인, 매출·매입 미리보기 표 컬럼 수 동일 확인. 신규 회귀 테스트 파일(e2e_ui_symmetry.js, 12개 케이스) 추가, PRINCIPLES.md에 원칙 21 추가(받는 쪽과 주는 쪽은 항상 같은 틀로 그릴 것 — 대칭이 스키마상 어려워도 기존 필드 인코딩으로 구현 방법을 먼저 찾을 것). 전체 스위트 97/97 통과.
[v2.9.31] 2026-09-14 — 견적 인건비 매입 UI 표준화(사용자 지적: 매출은 [단가]×[MM]=[총액]인데 매입은 총액만 덩그러니 있어 UI 표준 미준수) 작업 중 심각한 근본 결함 발견·수정. UI 표준 확립: 매출·매입은 화면에서 항상 [단가]×[MM 또는 수량]=[총액] 대칭 구조로 나열되어야 한다는 원칙을 세우고 사업소득형도 세금계산서형과 동일하게 매입 단가 입력→MM 자동 곱셈 구조로 통일(v2.9.26의 별도 buyAmount 총액 직접입력 필드는 폐기). 이 작업 중 estimate.html의 저장 로직(buy_price에 이미 계산된 총액을 저장)이 project.html·cashflow.html·input.html 전체가 공유하는 "S12.buy_price=단가(급여형만 예외)"라는 암묵적 계약과 어긋나 있었음을 발견 — 세금계산서형 매입까지 포함해 청구 스케줄 생성 시 정확히 MM배만큼 원가가 부풀려지는 근본 결함이었음(실제 재현: 14,000,000원이 28,000,000원으로 계산됨을 확인). 이 계약을 원상 복구해 buy_price는 급여형만 월별 확정 총액, 나머지(세금계산서형·사업소득형)는 항상 단가로 저장하도록 estimate.html의 저장·로드 로직을 전면 수정, js/calc.js의 v2.9.27 사업소득형 전용 균등배분 분기(이제 불필요해진 로직)도 제거. 검증: 스크린샷 실제 수치(단가 7,917,208×MM2=15,834,416)로 저장·재조회·청구스케줄 생성까지 전 구간 정확 확인, 세금계산서형(project.html 손익 14,000,000원 정확)·급여형 회귀 통과. 회귀 테스트를 새 계약 기준으로 전면 갱신하고, 신규 파일(e2e_project_buyprice.js)로 이 계약을 project.html·cashflow.html 양쪽에서 상시 감시하도록 함. PRINCIPLES.md에 원칙 20 추가(여러 화면이 공유하는 저장 필드의 의미 계약은 한 곳만 바꾸면 반드시 깨진다 — 배율만큼 어긋나는 결함은 처음엔 우연한 오차처럼 보이기 쉬움). 전체 스위트 85/85 통과.
[v2.9.29] 2026-09-14 — 견적서 사업소득형·급여형의 안내 문구가 옛 설계(v2.9.24, "청구 스케줄 생성 시 매입 목록에 포함되지 않음")를 그대로 남긴 채 방치되어 있던 결함 수정(사용자 질문: 매입 단가·금액을 어디서 확인할 수 있는지 알 수 없음). v2.9.27에서 매입 스케줄을 "지급형태 무관 전원 포함"으로 재설계했음에도 이 안내 문구만 갱신을 놓쳐, 화면이 실제 동작과 반대로 안내하고 있었음. 문구를 "세금계산서 화면의 청구 스케줄에는 포함되나 부가세는 0원 처리"로 정정. 참고: 견적서 화면 자체에는 이미 "인건비 소계"(지급형태 무관 실시간 합산)가 있어 이 화면 안에서도 매입 총액 확인이 가능함(기존 기능, 변경 없음). 전체 스위트 79/79 통과.
[v2.9.28] 2026-09-14 — 화면 헤더에 배포 버전 표시 신설(사용자 요청: 현재 실행 중인 버전을 화면에서 바로 구분할 수 있도록). js/nav.js에 HFC_VERSION 상수 신설(새 버전 배포 시 이 한 곳만 갱신하면 전 화면 자동 반영), 사이드바 하단에 "HFC 경영관리 vX.X.X" 표시, 데스크톱·모바일 모두에서 보이도록 각 화면 헤더(.app-logo 또는 .app-title)에도 작은 배지 주입 — 화면마다 헤더 클래스명이 app-logo/app-title로 갈리던 기존 비일관성 때문에 profile.html만 배지가 누락되던 것을 검증 중 발견해 선택자에 두 클래스 모두 포함해 해결. 10개 화면 개별 HTML 수정 없이 nav.js 공통 로직만으로 전체 반영. 검증: 헤더 구조가 서로 다른 3가지 패턴(표준 app-logo, fixed/project의 커스텀 로고+상태표시, profile의 app-title)에서 전부 배지 정상 표시 확인. 회귀 테스트 신설(e2e_nav_version.js, 10개 화면 전수 확인), 전체 스위트 79/79 통과.
[v2.9.27] 2026-09-14 — 견적 인건비 지급형태(v2.9.17~26)를 둘러싼 청구 스케줄·자금수지·부가세 연동을 전면 재설계(사용자가 전체 도메인 그림을 재확인하며 지적: 지급형태별로 세금계산서/부가세/자금수지 반영 방식이 다른데 이걸 당연히 알고 있어야 하는데도 프로그램이 반영 못하고 있었음). 핵심 설계 반전: v2.9.24가 "매입 스케줄 생성 대상=부가세 매입세액공제 대상"으로 두 가지 서로 다른 축(자금흐름 반영 여부·부가세 대상 여부)을 하나로 묶어, 세금계산서 없는 매입(사업소득형·급여형)을 매입 스케줄 자체에서 제외했던 것을 정정 — 매입 청구 스케줄(S05)은 지급형태 무관하게 전 인력 포함해야 함(자금 유출은 계산서 유무와 무관하게 실재, 월 추정 자금수지 정확성을 위해 필수). 세금계산서형만 vat_amt·invoice_plan_date를 채우고, 사업소득형·급여형은 vat_amt=0으로 자금수지엔 반영되되 부가세 매입세액공제에서만 제외되는 구조로 재설계. 변경 파일: input.html(genCtx의 eBuy 전체 재계산+eBuyInvoice/eBuyNonInvoice 분리 집계, buildGen·msCalc의 매입 스케줄을 세금계산서형/계산서없음 두 갈래로 생성, saveGen의 매입 레코드 생성부가 isInvoice 플래그로 cost_type·vat_amt·invoice_plan_date 분기, 미리보기 화면의 VAT 열이 계산서없음 항목에 "계산서없음" 표시), js/calc.js(monthlyProfile에 사업소득형 총액 균등배분 로직 추가 — v2.9.26에서 buy_price가 단가에서 총액으로 바뀐 후 이중계산 위험 재발 방지), vat.html(vatOf 함수가 vat_amt=0을 falsy로 오인해 amount*10%로 폴백하던 결함 발견·수정 — 계산서 없는 매입에도 부가세가 잡히는 심각한 오류로 이어질 뻔했음). 검증: 세금계산서형(외주 7백만×7)+사업소득형(총액 5,600만)+급여형(월별 1,680만) 3인 혼합 견적에서 매입 총액 121,800,000원 전액 자금수지 반영, 부가세는 세금계산서형분 4,900,000원만 정확히 반영(계산서없음 72,800,000원은 제외) 확인, 자금수지 화면의 계획출금이 지급형태 무관 전액 반영됨을 별도 확인. 회귀 테스트의 v2.9.24 관련 3개 파일(e2e_schedule, e2e_confirmed_data, e2e_vat)을 새 설계에 맞춰 전면 갱신, PRINCIPLES.md에 원칙 2개 추가(18: 서로 다른 두 분류축을 하나의 필터로 묶지 말 것, 19: 합계값 필드에서 0을 값없음으로 취급해 폴백시키지 말 것), 전체 스위트 69/69 통과.
[v2.9.26-docs] 2026-09-14 — tests/PRINCIPLES.md에 원칙 5개(13~17) 추가. 지급형태 설계가 반복적으로 사각지대를 드러낸 v2.9.24~26 구간에서 추출: ①요청받은 기능만 만들지 말고 그 도메인의 전체 경우의 수를 먼저 나열할 것(세금계산서형 누락 사례) ②의미가 다른 값을 한 필드에 겸용시키지 말 것(단가/금액 이중계산 사례) ③상태 변경으로 대상이 0건이 되는 경우도 정상 흐름에 포함해 액션 버튼을 잠그지 말 것(저장버튼 잠김 3회 반복 사례) ④도메인 지식만 있으면 판단 가능한 것은 사용자에게 되묻지 말 것 ⑤같은 실패가 반복되면 원칙의 적용 범위 자체를 의심할 것. tests/README.md의 커버리지 표·케이스 수(54→64)도 최신화. 코드 변경 없음(문서만 갱신).
[v2.9.26] 2026-09-14 — 사업소득형 매입금액이 저장 후 재조회하면 MM만큼 곱해져 늘어나는 결함 수정(사용자 신고, 해결책도 사용자 제안: 단가와 금액 필드 분리). 원인: v2.9.24에서 지급형태 3종을 도입하며 저장 로직은 "buy(단가)×mm=총액"을 buy_price에 넣도록 바꿨는데, 로드 로직은 세금계산서형과 동일하게 buy_price를 그대로 "단가" 칸에 복원 — 재조회 후 화면엔 이미 총액이 단가처럼 보이고, 여기서 다시 저장하면 그 총액에 mm을 또 곱해 이중 계산됨. 수정: 매입 입력을 지급형태별로 완전히 분리 — 세금계산서형=매입 단가(p.buy, MM과 곱셈), 급여형=월별 기본단가(p.buy, monthlyOverride로 개별월 예외, 기존 구조 유지), 사업소득형=매입 금액(p.buyAmount, 총액 그대로 사용, MM 곱셈 없음). laborBuyTotal() 함수로 지급형태별 계산을 한 곳에 통일(SSOT), 견적 요약·저장·변경이력 비교 등 모든 소비처가 이 함수를 거치도록 정리. 검증: 사업소득형 매입금액(9,369,234원)이 저장→재조회를 반복해도 MM(1)과 무관하게 그대로 유지됨을 확인(신고된 버그 정확히 재현 후 해결 확인), 세금계산서형(단가×MM)·급여형(월별 오버라이드) 기존 동작 회귀 통과. 회귀 테스트에 이 시나리오 명시 고정, 전체 스위트 64/64 통과.
[v2.9.25] 2026-09-14 — 청구 스케줄 저장 버튼이 지급형태 변경 시 다시 잠기던 결함 수정(사용자 반복 지적: 세금계산서 발행에서 사업소득으로 지급형태를 바꿔도 기존 매입 스케줄을 갱신할 방법이 없음). 원인: syncSaveBtn()이 "새로 생성할 매출·매입이 있는가"만 보고 저장 버튼을 열고 닫았는데, 지급형태를 세금계산서형→사업소득형으로 바꾸면(v2.9.24) 매입 대상이 0건이 되어 "기존 스케줄을 정리해야 하는 상황"(v2.9.11의 정리 확인 경로) 자체에 saveGen()이 호출되지 못하고 저장 버튼이 잠겨버림 — 매출 체크 해제+매입 체크만 있는 조합에서 특히 재현. 수정: genDoCost가 체크된 채 canCost=false(정리 대상 있을 수 있는 상태)이거나 genDoRev가 체크된 채 매출 기준이 없는 경우도 저장 버튼을 열도록 조건 확장, saveGen() 내부의 기존 정리 로직(v2.9.11)이 정상적으로 판단하게 함. 검증: 과거 세금계산서형 매입 스케줄 3건이 있는 상태에서 지급형태를 사업소득형으로 변경 후 매출 체크 해제+매입만 체크 → 저장 버튼 정상 활성화, 저장 시 과거 3건 정확히 정리됨을 확인. 이 시나리오를 회귀 테스트(e2e_confirmed_data)에 명시적으로 고정 — 같은 패턴 재발 방지. 전체 스위트 61/61 통과.
[v2.9.24] 2026-09-14 — 견적서 인건비 지급형태 설계 결함 수정(사용자 강한 지적: 세금계산서 발행분이 견적에서 누락되어 있었음, 업무 수행 능력에 대한 정당한 문제 제기). 인정: 급여형(4대보험)·사업소득형(3.3%) 두 가지만 만들고, 실무상 반드시 있어야 할 세 번째 유형 "세금계산서형(사업자 외주, 계산서 발행)"을 처음부터 빠뜨린 설계 누락. 지급형태를 3종으로 확장: 세금계산서형(기본값, 계산서 발행)/사업소득형(3.3% 원천징수, 계산서 없음)/급여형(4대보험, 계산서 없음). 핵심 원칙: 매출 계산(project 손익, cashflow 추정)은 지급형태 무관하게 전 인력 포함하되, 매입 스케줄(input.html의 S05 자동생성, 부가세 매입세액공제 대상)은 세금계산서형만 대상으로 함 — 급여형·사업소득형은 계산서 자체가 없어 매입 목록에 오르면 부가세 화면에서 실제로 없는 매입세액공제가 잡히는 심각한 오류로 이어질 위험이 있었음. genCtx()에 isInvoiceType 필터 신설, memo 태그 체계 확장(공백=세금계산서형/기존데이터, "사업소득형"=명시 태그, "급여형|{JSON}"=월별지급액). 검증: 세금계산서형·사업소득형·급여형 3인 혼합 견적에서 매출은 3인 전원(161,000,000) 포함, 매입 스케줄은 세금계산서형(외주업체)만(49,000,000) 정확히 반영, 급여형만 있으면 매입 생성 자체가 비활성화됨을 확인, 프로젝트 손익 화면은 지급형태 무관하게 3인 전원 원가(121,800,000) 정상 집계 확인. 회귀 테스트(e2e_estimate, e2e_schedule, e2e_vat)를 새 설계에 맞춰 갱신, 전체 스위트 59/59 통과.
[v2.9.23] 2026-09-14 — 급여형 인건비의 논리 오류 수정(사용자 지적: 급여형을 여러 달로 입력해도 MM=1 고정 때문에 총액이 반영 안 됨). v2.9.18에서 "급여형은 MM=1 고정"으로 이중 일할계산을 막았는데, 이게 "1개월만 입력하는 경우"만 가정한 설계였고 여러 달에 걸친 급여형 인력의 총액이 누락되는 새로운 논리 오류를 만들었음을 인정. 근본 재설계: 급여형은 시작·종료일 사이의 각 달을 나열하고, 월별 지급액을 개별 입력 가능하게 함(기본값=매입단가 그대로, 만근 아닌 달(입사·퇴사월)만 사용자가 실제 정산액으로 수정) — 사용자 확정 방식. 데이터 흐름: estimate.html이 월별 완전한 맵(오버라이드 안 한 달도 단가로 명시)을 memo="급여형|{JSON}"으로 저장, buy_price는 월별 합계 총액. 청구 스케줄 생성(js/calc.js monthlyProfile)은 이 JSON을 감지해 mmMonthly(일할계산) 대신 월별 금액을 그대로 각 달에 반영 — 매출(unit_price) 계산에는 이 로직이 적용되지 않음(지급형태 무관하게 항상 일할계산, 사용자 확인사항과 무관). 검증: 9월15일 입사(부분월 실정산 120만)+10~12월(만근 240만×3)=840만원이 견적 저장·재조회·청구스케줄 생성까지 전 구간에서 월별 금액 그대로(총액만이 아니라 각 달 개별 금액까지) 정확 반영 확인, 사업소득형 기존 정밀 일할계산 회귀 통과. 회귀 테스트(tests/e2e_estimate.js, e2e_schedule.js)를 새 설계 기준으로 갱신, 전체 스위트 58/58 통과.
[v2.9.22-tests] 2026-09-14 — 회귀 테스트 스위트 신설(사용자 지적 반영: "매번 새로 검증해야 한다"는 근본 문제 해결). 지금까지 즉석 스크립트로만 검증하고 버렸던 것을 tests/ 디렉토리에 정식 파일로 축적 — v2.9.9~v2.9.22에서 실제 발생한 결함 전부를 재현 가능한 테스트 케이스로 고정(총 54개, 7개 파일). 구조: calc_*.js(순수 함수, vm 로드, 빠름)와 e2e_*.js(실제 화면 조작 재현, jsdom)로 분리, run_all.js로 전체 일괄 실행. 커버 범위: MM 일할계산 실무공식(calc_mm) · 콤마 타입 안전성 및 S06 신규필드(calc_types) · 저장버튼 undefined·매입 직접입력 타이밍버그·_EMPTY 마커 오염(e2e_schedule) · 확정 데이터 보호 원칙·마일스톤 저장복원(e2e_confirmed_data) · 빈 상태 마커·지급형태 구분·급여형 이중계산 방지(e2e_estimate) · 부가세 확정값 잠금·기타경비 매입세액(e2e_vat) · 추정 재계산 배너(e2e_cashflow). 추가로 PRINCIPLES.md에 이번 결함 이력에서 추출한 12개 프로젝트 독립적 개발 원칙 정리(사용자가 회사 시스템 확장용 스킬로 활용 예정 — 확정데이터 필터링, 체크박스 disabled 원칙, SSOT, 타입 단일관문 방어, 신규/의도적삭제 구분, 실무규칙 예시검증, 원인규명 방법론 등). 다음 결함 수정 시 절차: run_all.js 선실행 → 새 케이스 추가 → 재실행, zip에 tests/ 디렉토리 항상 포함.
[v2.9.22] 2026-09-14 — 기타경비(고정비) 세금계산서 매입세액 반영 신설(사용자 지적: 실제 납부세액이 시스템 계산과 다른 이유는 프로젝트 매입 외에 임대료·통신비 등 기타경비 세금계산서가 부가세 계산에 전혀 반영되지 않았기 때문). 결정 확정: 발생 형태=비정기, 반영 위치=고정비 항목 자체(별도 화면 신설 대신). S06_FIXED_COST에 has_tax_invoice(세금계산서 수취 Y/N)·vat_amt(부가세, 직접 입력) 2개 컬럼 추가(schema.js 헤더·범위 A1:F→A1:H 동기화, NUMERIC_COLS·ZERO_DEFAULT_COLS는 vat_amt 기존 등록으로 자동 안전). fixed.html: 등록·수정 폼에 "세금계산서 수취" 체크박스+부가세 입력칸 추가(체크 시에만 노출), 목록에 "계산서" 뱃지로 시각 표시, 신규 등록·단건 수정 양쪽 저장 경로 반영. vat.html: calcQ가 프로젝트 매입(S05)뿐 아니라 해당 분기 월에 속하는 고정비 중 세금계산서 수취분(S06)도 매입세액에 합산, 미확정 분기 카드에 "프로젝트 OOO원 + 기타경비 OOO원" 구성 세부 표시로 투명성 확보. 검증: 임차료 세금계산서(부가세 5만원)가 매입세액에 정확히 합산되고 계산서 없는 통신비는 제외됨을 확인, fixed.html 저장·수정 전 구간 정상 확인, 기존(신규 필드 없는) 고정비 데이터 회귀 통과.
[v2.9.21] 2026-09-14 — 부가세 관리 화면의 "신고 확정" 값 잠금 및 사후 변경 경고 신설(사용자 질문·의사결정 반영). 배경: 부가세 카드는 매번 그 분기(3개월)의 매출·매입을 S04·S05에서 실시간 재계산해 보여주는데, 발행·입금 여부와 무관하게 전부 합산하는 구조라 [납부 완료 처리]로 이미 세무서에 신고·납부한 분기라도 이후 청구 스케줄이 바뀌면 화면 숫자가 조용히 달라져 보이는 위험이 있었음(실제 신고 내용을 알 수 없게 되거나 오류로 오인할 위험). 결정 원칙: "부가세는 실제로 신고·납부하는 돈이므로 확정 후에는 숫자를 함부로 바꿔 보이면 안 된다"는 원칙에 따라, 이미 존재하던 저장 구조(upsertVat이 [납부 완료 처리] 시점의 계산값을 S13에 확정 기록)를 그대로 살리되 화면 표시 레이어를 신규 구현: "납부완료" 상태인 분기는 그 확정 저장값을 그대로 표시("(신고 확정)" 라벨), 이후 실제 데이터가 바뀌어 실시간 재계산값과 달라지면 카드 하단에 빨간 경고("신고 이후 매출·매입 내역이 변경되었습니다 — 수정신고 필요 여부 확인")와 함께 최신 재계산값을 병기. 아직 납부 전(집계중·납부예정) 분기는 기존대로 실시간 재계산값을 그대로 노출(청구 스케줄 변경이 즉시 반영되는 게 실무에 맞음). 검증: 변경 없는 납부완료 분기(경고 없음), 확정 후 매출이 바뀐 분기(경고+최신값 표시), 미확정 분기 회귀(기존 실시간 표시 유지) 전부 확인.
[v2.9.20] 2026-09-14 — 자금수지 "추정 재계산 필요" 안내 배너 신설(사용자 확인 질문에서 시작). 배경: "월별 계획 vs 실적" 표는 S04·S05를 매번 실시간으로 다시 읽어 자동 최신화되지만, "추정 자금수지"(S02, 월별 유입/유출/잔액 요약)는 [추정 재계산 실행] 버튼을 눌러야만 갱신되는 별도 스냅샷이라 청구 스케줄이 바뀌어도 자동 반영되지 않음 — 사용자가 모르면 예전 수치를 계속 보게 되는 위험. 구현: 청구 스케줄 생성(saveGen 성공 시) S10_SETTINGS에 마지막 변경 시각(last_schedule_change_at) 기록, 자금수지 진입 시 이 시각과 S02 각 행 updated_at의 최댓값을 비교해 스케줄 변경이 더 최신이면 상단에 노란 배너와 [지금 재계산] 버튼 표시. 재계산 실행 시 즉시 사라짐. 스케줄 변경 이력 자체가 없는 기존 데이터는 판단을 보류(배너 미표시)해 오탐 방지. 검증: 스케줄변경>재계산 시 배너 표시, 재계산>스케줄변경 시 미표시, 이력 없음 시 미표시, 재계산 버튼 클릭 후 실제 시트 갱신 반영 시 배너 정상 소멸 확인.
[v2.9.19] 2026-09-14 — MM 일할계산 공식을 실무 방식으로 전면 재정의(사용자 지정, 기존 SI 관례 방식은 실무와 다름을 확인 후 교체). 신규 공식: 완전월(만근)=1.00, 시작월(중간투입)=(그 달 말일-투입일+1)/그 달 총일수, 종료월(중간철수)=철수일/그 달 총일수, 총MM=시작월+중간완전월 개수+종료월. 구 방식(투입일 기준 "d일~d-1일=1개월" 앵커)과는 3/12~9/11 같은 표준 사례에서 실제로 다른 값이 나옴을 검증(구 6.00 vs 신규 6.01) — 우연한 차이가 아니라 방식 자체가 다름을 확인 후 교체. mmMonthly(월별 배분)는 시작월을 실무 공식으로 독립 계산하되, 마지막 달은 총MM에서 앞선 달들의 합을 뺀 잔여값으로 역산(사용자 확정) — 각 달을 모두 독립 반올림하면 총합이 mmTotal과 0.01 정도 어긋날 수 있어(예: 6.02 vs 6.01), 오차를 마지막 달에 흡수시켜 총 MM과 월별 배분 합계가 항상 정확히 일치하도록 함. 급여형(v2.9.18, MM=1 고정)은 이 변경과 무관하게 그대로 유지 — 회귀 확인. 검증: 사용자 제시 예시(2/25~2/28=0.14, 3/25~3/31=0.23) 정확 일치, 이무헌 3/12~9/11=6.01·권용현 3/12~11/11=8.01(구 6.00/8.00에서 갱신), 월별분해 합계가 총MM과 소수 둘째자리까지 정확히 일치, 청구 스케줄 생성(견적단가×총MM)까지 종단간 정확 확인.
[v2.9.18] 2026-09-14 — 견적서 급여형 인건비의 일할계산 이중 반영 방지(사용자 지적: 매월 첫날 투입·말일 철수가 보장되지 않는 구간을 고려했는지). 확인 결과 기존 mmTotal(투입~철수 정밀 월할)은 정상 동작하나, 급여형은 회사 급여대장에서 이미 일할 계산이 끝난 실지급액을 그대로 입력하는 방식(사용자 확정)이라 여기에 mmTotal의 소수 MM을 다시 곱하면 이중 일할계산이 되어 금액이 왜곡되는 것을 확인. 수정: 급여형 선택 시 MM을 항상 1로 고정(입력 필드 비활성화), 지급형태를 급여형으로 전환하는 즉시 MM=1 적용, 사업소득형으로 되돌리면 시작·종료일이 남아있는 경우 정밀 일할계산 재적용. 저장된 시트를 불러올 때도 급여형이면 MM을 1로 강제(과거 오입력 방어). 사업소득형은 기존 mmTotal 기반 정밀 일할계산 완전히 유지(회귀 없음). 검증: 9월 15일 입사처럼 어중간한 날짜를 급여형에 입력해도 이중계산 없이 실지급액 그대로 반영, 사업소득형 기존 정밀계산(3/12~9/11=6.00개월) 회귀 통과, 지급형태 전환 왕복 시 MM 정확 복원, 급여형+사업소득형 혼재 저장까지 최종 확인.
[v2.9.17] 2026-09-14 — 견적서 인건비에 지급형태 구분 신설(사용자 요청: 한 인력이 급여형 4대보험 + 사업소득형 3.3% 두 형태로 동시에 지급되는 경우를 반영할 방법이 없어 매입견적을 0으로 두고 고정비로 우회 입력하려던 상황 — 프로젝트 손익에서 해당 인건비가 누락되는 문제 해소). 설계: 인력별로 "사업소득형(3.3% 원천징수)" / "급여형(4대보험)" 선택 추가, 같은 사람을 두 행으로 나눠 등록(행 단위 유지 — 자동 합산 표시는 안 함). 금액은 형태와 무관하게 사용자가 직접 입력한 값 그대로 원가 반영(급여형은 기본급+4대보험 회사부담금+소득세 원천징수까지 포함한 회사 실지급 총액을 입력하도록 안내 문구 표시) — 4대보험 요율 자동계산 없이 매번 사용자 입력이라는 확정 방침에 따라 계산 로직은 전혀 변경하지 않음. 스키마 변경 없이 기존 인건비 행의 항상 공백이던 memo 필드를 재사용해 지급형태를 저장(급여형="급여형", 사업소득형=공백 — 기존 데이터와 완전 호환). project.html·cashflow.html·input.html의 견적 합계 로직은 comp_type만 보고 집계해 별도 수정 없이 두 행이 정확히 합산됨을 확인. 검증: 한 사람 두 행(사업소득형 700만+급여형 239만) 저장·재조회 시 형태 정확히 복원 및 합산 원가 정확, 기존(memo 없음) 데이터 회귀 통과.
[v2.9.16] 2026-09-13 — v2.9.15에서 추가한 "매입 직접 입력" 기능의 저장 버튼이 눌리지 않던 결함(사용자 신고). 원인: buildGen()이 매입 생성 불가 상태(canCost=false)일 때 매입 체크박스를 disabled뿐 아니라 checked=false로도 강제로 껐는데, "견적 매입 합계"에서 "직접 입력"으로 라디오를 전환하는 바로 그 순간(아직 금액을 입력하기 전, applyBuy=0)에도 이 조건을 타 체크박스가 꺼졌고, 이후 금액을 입력해 canCost=true가 되어도 이미 꺼진 checked는 되돌아오지 않아 매입 청구 생성 자체가 비활성 상태로 굳어짐. 수정: disabled만으로 조작을 제한하고 checked는 사용자가 직접 조작하지 않는 한 건드리지 않음(비활성 상태에서는 값이 반영되지 않으므로 안전). 검증: 실제 사용자 조작 순서(매출 체크 해제 → 매입 기준을 직접입력으로 전환 → 금액 입력) 그대로 재현해 저장 버튼 정상 작동 및 매입 3건(합계 70,000,000원) 저장 확인, 견적 정상 존재 시 기존 동작 회귀 통과, 진짜 생성 불가능한 초기 상태의 비활성화도 정상 유지 확인.
[v2.9.15] 2026-09-13 — 매입 청구 스케줄이 "견적 기준" 한 가지 경로만 있어, 견적이 없거나 지워지면 이미 존재하는 매입 스케줄을 갱신할 방법이 아예 없던 설계 공백 수정(사용자 지적: 최초 생성과 이후 수정을 구분 안 해 매번 막힘). 매출은 계약금액이라는 항상 존재하는 기준이 있어 견적 없이도 회차 생성이 되는데, 매입은 그런 대안이 없었던 게 근본 원인. 매입 기준에 "직접 입력" 옵션 신설(매출 기준의 계약금액/견적 매출 합계 라디오와 대칭) — 총액을 직접 입력하면 견적 유무와 무관하게 회차별(마일스톤) 또는 기간 균등 분배로 매입 스케줄을 생성·갱신할 수 있다. genCtx에 canCost(매입 생성 가능 여부)·applyBuy(적용 매입 총액) 추가해 견적 유무와 매입 생성 가능 여부를 분리, msCalc·buildGen·saveGen의 v2.9.11 방어 조건을 canCost 기준으로 통일. 검증: 견적 없는 상태에서 직접 입력(70,000,000원)으로 매입 3건 정상 갱신, 매출 체크 해제 시 S04는 완전히 호출되지 않아 기존 매출 데이터 무영향 확인, 견적이 정상 존재하는 기존 케이스 회귀 통과.
[v2.9.14] 2026-09-13 — v2.9.13이 만든 후속 결함 수정(사용자 신고: 매입단가 0으로 변경 후 스케줄 업데이트 시 저장 불가). 원인: v2.9.13에서 견적을 의도적으로 비울 때 남기는 _EMPTY 마커가, 세금계산서의 청구 스케줄 생성 로직(genCtx)에서 "실제 견적 항목"으로 잘못 집계되어 hasEst=true·eSell=0·eBuy=0·적용 매출기준=0원이 되고 "기간·금액이 없어 생성할 수 없습니다"로 매출·매입 스케줄 생성 자체가 막힘. 수정: estOf() 함수 자체에서 _EMPTY 마커를 제외하도록 SSOT 적용(이 함수를 쓰는 모든 곳이 자동으로 안전해짐), cashflow.html의 estRatio 직접 조회 지점도 동일 필터 적용. 검증: 마커만 있는 상태에서 견적 없음으로 정확히 판정되어 계약금액 기준 매출 스케줄 정상 생성, v2.9.11의 "매입 불가 시 과거 스케줄 정리 확인" 방어와 정상 연계 확인, 실제 견적 항목이 있는 정상 케이스 회귀 통과.
[v2.9.13] 2026-09-13 — 견적 화면의 동일 계열 설계 결함 수정(사용자 지적: "이것도 마찬가지 오류"): 기존 인력·항목을 전부 삭제하고 저장한 뒤 다시 열면, "저장 이력 없음(신규)"과 "저장했지만 항목을 전부 지움(의도적 삭제)"을 estimates 배열만으로 구분할 수 없어 매번 등급단가가 채워진 빈 인력 행이 자동으로 부활하던 결함(loadOpp의 !labor.length 조건이 두 상태를 동일하게 취급). 1차 조치로 세션 내 저장 이력 추적(savedOnceThisSession)을 추가했으나 새로고침·재접속 시 여전히 재현되는 한계를 발견해, 근본 해결로 전환: 항목을 전부 삭제하고 저장하면 comp_type=_EMPTY인 빈 상태 마커 행을 S12에 남겨 "저장 이력은 있으나 의도적으로 비움"을 명시적으로 표시. 다른 화면(project/input/cashflow)의 견적 합계 계산은 comp_type이 매칭되지 않아 마커가 자동으로 0원 처리되어 영향 없음. 검증: 같은 세션·새로고침 후 재조회 양쪽에서 빈 행 부활 없음 확인, 진짜 신규 견적의 편의 기능(첫 진입 시 빈 행 자동 추가)은 회귀 없이 유지, 마커의 타 화면 금액 계산 무영향 확인.
[v2.9.12] 2026-09-13 — 청구 스케줄 생성(용역턴키 마일스톤 방식)의 설계 결함 수정(사용자 지적): 회차 수·비율·기준일이 어디에도 저장되지 않아 모달을 다시 열 때마다 하드코딩된 기본값(30/40/30% 등)으로 항상 초기화되던 문제 — 최초 생성 이후 회차 조건을 수정하는 게 실무상 당연히 필요한데 그 경로가 없었음. 신규 시트 S17_MILESTONE 추가(project_id별 1행, 회차 설정을 JSON으로 저장). saveGen()이 용역턴키 프로젝트의 회차 설정을 저장(시트 미생성 시 자동 생성 후 재시도), ensureMsRows()가 저장된 설정이 있고 회차 수가 일치하면 그 값으로 복원, 없거나 회차 수가 바뀌면 안전하게 기본값 폴백. 검증: 커스텀 비율(25/45/30%)·수정 기준일 저장 후 재조회 시 정확히 복원 확인, 저장 이력 없음/회차 수 변경 시 기본값 폴백 확인, 기존 청구 스케줄 생성(계약금액 합계 정확) 회귀 통과.
[v2.9.11] 2026-09-13 — 청구 스케줄 재생성 시 견적 소실(매입 견적 삭제 등)로 매입/매출 체크박스가 비활성화되면 그 항목의 저장 로직 자체를 건너뛰어, 과거에 이미 생성된 스케줄이 세금계산서 목록에 방치되던 결함(사용자 신고). saveGen이 doCost/doRev가 false인 경우를 "손대지 않음"으로만 처리해 대칭성이 깨져 있었음(재생성 불가=계획 소멸 의미인데 과거 산출물은 그대로 남는 모순). 수정: 매입 생성이 불가능한 상태(견적 없음)에서 해당 프로젝트의 기존 미수취 매입이 있으면 삭제 여부를 확인 후 정리, 매출 측도 기간·계약금액 소실 시 동일 원칙으로 대칭 방어. 확인창에서 취소 시 기존 스케줄은 그대로 보존(불필요한 쓰기 없음). 검증: 스크린샷과 동일 시나리오(매입 견적 삭제 후 재생성) 재현 — 확인 후 정리/취소 시 보존 양쪽, 견적 정상 존재 시 기존 정상 동작 회귀 전부 통과.
[v2.9.10] 2026-09-13 — 청구 스케줄 생성 미리보기에서 금액을 직접 수정하면 undefined가 되어 저장이 완전히 막히던 결함(사용자 신고): updRev·updCost 함수가 fc(el)(엘리먼트를 받아 그 자리에서 값을 바꾸는 함수, 반환값 없음)를 fc(el.value)(문자열을 넘기고 반환값을 기대하는 방식)로 잘못 호출해 el.value=undefined가 대입되던 오래된 잠복 결함. saveGen의 필수값 검사(!g.supply)가 undefined를 걸러내며 "금액을 확인하세요" 안내와 함께 저장을 차단 — 이것이 "무엇을 고치면 다른 게 안 된다"는 증상의 정체(신규 버그 아님, 이번에 처음 그 입력 경로를 사용하며 드러난 기존 결함). comma()를 직접 호출하도록 교정(2곳). 검증: 매출·매입 미리보기 양쪽에서 금액 수정 후 화면 표시·내부값·최종 저장까지 전 구간 정상 확인.
[v2.9.9] 2026-08-12~13 — 데이터 타입 표준화 설계서(v1.0) 승인 반영. 5개 결정 확정 구현: ①공백 처리=컬럼 성격별 차등(금액·집계류는 ZERO_DEFAULT_COLS로 공백시 0, mm·qty 등 "값 없음"이 의미 있는 컬럼은 공백 보존) ②S10_SETTINGS 헤더 불일치 즉시 수정(schema.js 정의를 실제 저장 헤더 setting_key/setting_value로 통일 — saveCodes 함수도 구 key/value 헤더를 쓰고 있던 추가 불일치 함께 발견·수정) ③읽기 방어 전체 적용(parseInt/parseFloat(obj.field) 형태 전역 132곳을 안전 헬퍼 num()/numF()로 순차 교체, 화면별 회귀 검증) ④기존 시트 데이터 자동 마이그레이션(별도 스크립트 없이 다음 저장 시 rowsFor()가 자동으로 순수 숫자형 재기록) ⑤이력서 자유형식 컬럼(S07.birth, S16.start/end) 보존 유지(FREE_TEXT_DATE_COLS로 명시).
핵심 인프라 신설: js/schema.js에 NUMERIC_COLS·ZERO_DEFAULT_COLS·FREE_TEXT_DATE_COLS(SSOT), js/common.js에 num()·numF()·rowsFor()(객체→시트행 변환 단일 관문 — 숫자 컬럼은 항상 콤마 제거된 순수 number로 저장, Google Sheets 셀이 실제 숫자형이 되어 SUM/AVERAGE 등 시트 자체 함수 사용 가능).
전 10개 화면(input/cashflow/estimate/fixed/settings/pipeline/vat/profile/index/project) 저장·읽기 로직 전면 교체 및 화면별 회귀 검증 완료. 근본 원인 추적 중 js/calc.js의 monthlyProfile에서 v2.9.7~8 "백만분의 1 축소" 사고의 실제 발생 지점(parseInt(e[priceField]) 콤마 미방어)을 발견해 수정.
아키텍처 결함 2건 추가 발견·수정: ①profile.html의 schema.js/common.js 로드 순서가 반대여서 타입 메타를 못 읽던 결함 ②estimate.html의 mmBetween 함수가 js/calc.js의 mmTotal과 완전히 동일한 로직으로 중복 정의되어 있던 SSOT 위반(아키텍처 표준 위반) — mmBetween 제거, calc.js의 mmTotal로 통일. 또한 calc.js가 input.html에 정의된 pad2·ymList에 의존하던 모듈 독립성 결함도 발견해 두 함수를 calc.js로 이전(SSOT 원칙 복원).
최종 종합 회귀: MM 계산 3인 예제 스펙 7/7 통과, 콤마 포함 견적단가+MM계산+월별 라운딩 배분이 맞물린 청구 스케줄 생성에서 계약금액과 1원 단위까지 정확 일치 확인.
[v2.9.8] 2026-08-12 — v2.9.7 진단 정정 및 근본 원인 재수정. v2.9.7의 XLSX 서식 진단은 틀렸음(사용자 확인: 세금계산서 화면 등 XLSX와 무관한 곳에서도 동일 현상 발생). 정확한 원인: 코드 전역 70여 곳에서 parseInt(obj.field) 형태로 금액을 파싱하는데, JS의 parseInt("13,200,000")는 콤마에서 파싱을 멈춰 "13"만 반환한다(자릿수 대량 손실 — 사용자가 "백만원 단위로 보인다"고 표현한 현상과 정확히 일치). 화면 입력은 항상 un()/uncomma()로 안전하게 처리되지만, 구글시트 셀에 사용자가 직접 콤마 포함 텍스트(예: "13,200,000")를 입력하면 그 방어를 우회해 코드 전역의 무방비한 parseInt 호출이 값을 훼손한다. 근본 수정: 시트→객체 변환의 유일한 관문인 toObj()(js/common.js)에서 알려진 금액/수량 컬럼(AMOUNT_KEYS 목록)의 콤마를 읽기 즉시 제거해 순수 숫자 문자열로 정규화 — 이후 70여 곳의 parseInt 호출이 전부 안전해짐(개별 수정 불필요, SSOT 방어). 검증: 콤마 포함 시트 값(세금계산서 13,200,000·자금수지 계획입금 14,520,000·견적 단가 9,090,909)이 정규화 후 전부 정확히 표시됨을 데이터 레벨과 화면 렌더 양쪽에서 확인. 복구 절차: 코드 배포만으로 해결(시트 재업로드 불필요) — 기존에 콤마 포함으로 저장된 시트 값도 다음 로드부터 정상 표시된다.
[v2.9.7] 2026-08-12 — 자금수지 금액이 백만분의 1로 표시되던 심각한 결함 진단·수정. 원인 확정: XLSX 업로드 파서가 SheetJS를 raw:false로 읽는데, 엑셀 셀에 커스텀 표시서식(#,##0,, 형태 — 백만원 단위로 축약해 보여주는 서식)이 적용되어 있으면 raw:false는 실제 값(예: 20,000,000)이 아니라 서식이 적용된 문자열("20")을 반환함(SheetJS 정상 동작, 우리 코드가 이를 대비하지 못함). CSV 업로드는 서식 개념이 없어 영향 없음 — XLSX 업로드에서만 발생. 수정: XLSX 읽기 시 서식 텍스트(raw:false)와 원본 숫자값(raw:true)을 함께 추출, 금액 3개 컬럼(입금·출금·잔액)은 원본 숫자값을 우선 사용하도록 변경(문자열 파싱은 숫자 셀이 아닐 때만 폴백). 재현 검증: 동일 왜곡서식 파일로 수정 전 재현(20,000,000→"20") 및 수정 후 정상값(20,000,000) 추출 확인, 무서식 정상 파일 회귀 확인. 복구 절차: 전 기간 CSV/XLSX 재업로드(병합 저장이라 안전) 후 추정 재계산.
[v2.9.6] 2026-08-11 — UI 표준 위반 시정: Step2 개발 시 요소별 표준(용어·포맷 표기) 선언 없이 6개 섹션 필드를 즉흥 작성한 것이 원인(같은 "종료"가 섹션마다 졸업월/종료월로 의미가 다르고, "시작(YYYY.MM)"과 "시작"이 혼재하던 불일치). SECS를 [key,라벨,placeholder,폭] 구조의 단일 메타(SSOT)로 재정의해 용어·포맷 규칙을 명문화: 날짜류는 라벨에 시작월/종료월/취득월/졸업월로 단위 명시+placeholder에 YYYY.MM 포맷 예시, title/org/role/detail류는 실제 예시값을 placeholder로 제시. 상세보기 표·등록폼·자동매핑 결과가 전부 이 단일 정의를 공유(향후 라벨 변경은 SECS 한 곳만 수정).
[v2.9.5] 2026-08-11 — 인력 등록 폼 UX 개선(사용자 지적 반영): ①모든 신상 입력칸에 라벨 추가(placeholder만 있어 값 입력 시 무슨 칸인지 알 수 없던 문제 — 라벨은 값 입력 후에도 유지), 6개 섹션 에디터에도 컬럼 헤더 행 추가(회사명·기간·직위·담당업무 등) ②docx 자동 채움 시 원본 문서 내용을 화면 안에 바로 표시하는 대사 패널 신설(펼침 접이식, 파일 처리 즉시 자동 펼침) — 원본을 별도로 열지 않고 화면 안에서 폼과 대조하며 오류를 바로 수정 가능해 동선 단축.
[v2.9.4] 2026-08-11 — 이력서 자동 매핑 3차 양식 대응 + 정확도 개선(실파일 확인, 추정 배제 원칙 적용). 신규 양식(유훈 건): 신상 표에 생년월일 대신 주민등록번호(→ 생년월일 자동 환산), 학력이 라벨 없는 자유텍스트, 프로젝트 표가 헤더 2행(제목행+개발환경 세부헤더행) 구조 — 전부 대응. 회귀 중 발견한 기존 결함 2건도 함께 교정: ①프로젝트 데이터 판별을 날짜 정규식(YYYY.MM~)에 의존해 시작일·종료일이 별도 컬럼인 양식(이용주 건)에서 전체 누락되던 결함 → 헤더 키워드 명시 판별(다중 헤더 행 합산)로 전환 ②학력/자격증 헤더 행 자체가 데이터로 오인식되거나(최용민) 세부헤더 행이 프로젝트로 섞이던(정혜림) 결함 → 헤더 종료 지점(hEnd) 계산 후 그 이후만 데이터로 처리. 실샘플 5건 전체 회귀: 최용민31·이용주15·정혜림37·김덕수83·유훈49건, 세부 항목 셀 단위 검증 완료.
[v2.9.3] 2026-08-11 — 인력 프로필 저장 시 "Google 로그인이 필요합니다" 안내만 뜨고 진행이 안 되던 결함: saveProfile·createProfileSheets에 accessToken 선제 차단문이 있어, 다른 화면들과 달리 clearAndWrite/ensureSheet의 자동 로그인 팝업 기회를 가로막고 있었음(중복·역행 코드). 선제 차단 제거 — 이제 로그인 안 된 상태에서 저장을 눌러도 자동으로 Google 로그인 팝업이 뜨고, 승인하면 재시도 없이 그대로 저장됩니다. 상단 로그인 버튼에 로그인 상태 표시(✓ 로그인됨) 추가.
[v2.9.2] 2026-08-10 — 검증 태만 인정 및 재발 방지: 어제 제공된 실샘플 4건 중 1건만 실제 구조 확인 후 코드화하고 나머지는 유사할 것이라 추정한 채 진행한 것이 김덕수 건 실패의 근본 원인. 재발 방지책: 자동 매핑 결과가 이름 미인식이거나 6섹션 중 4개 이상 0건이면 "처음 보는 양식" 경고를 명확히 표시(빨간 배너)하여, 조용한 부분 실패 대신 사용자가 즉시 알 수 있게 함. 정상 인식 시에는 기존 파란 배너 유지.
[v2.9.1] 2026-08-10 — 이력서 자동 매핑 결함(사용자 신고 실파일 재현 확정): "세로 나열형" 양식(신상~학력~경력이 표 하나에 라벨-값 행으로 순서대로 이어지고, 상단에 라벨 없는 "이름/직위" 행이 별도 존재하는 구조 — 최용민 표준과 다른 배치)에서 이름에 "영문명:" 접두가 섞이고 학력·경력 인식 실패, 6개 섹션이 모두 0건으로 나오던 결함. mapResumeTables에 세로형 감지 로직(vertTable) 신설: 라벨 없는 최상단 "이름/직위" 행 우선 인식, "학력 사항"/"경력 사항" 구분 행 이후를 모드 전환으로 파싱, 서브헤더 행(학교/회사명/기간 등) 스킵, IT자격증·연락처(☎/E-mail 3칸 배치)·S/W등급(특급 등) 별도 추출. 기존 최용민 표준 양식 매핑과 공존(간섭 없음). 회귀 검증: 실샘플 4건(최용민30·이용주22·정혜림35·김덕수83건) 전부 정확 추출, 신규 파일 브라우저 E2E(드롭→저장) 통과.
[v2.9] 2026-08-10 — 인력 프로필 Step3: docx 자동 매핑 구현. mammoth.js(CDN)로 워드 표를 브라우저에서 직접 파싱해 신상(성명·생년월일·성별·경력·병역)·연락처(주소·이메일·전화)·6개 반복 섹션(학력/자격증/경력/교육/기술/프로젝트)을 폼에 자동 채움, 결과 요약 배너로 인식 건수 표시 후 사용자가 확인·수정 후 저장. 실샘플 4건 기준 검증(경력12·프로젝트10건 등 정확 추출, 정혜림 프로젝트 31건까지 처리). rowspan 병합 셀(교육 표)은 원본 특성상 완전 분리 불가 — "확인 필요" 표시로 정직하게 노출. 결함 수정 겸용: ①CDN 스크립트가 defer 없이 동기 차단으로 삽입되어 로드 실패 시 이후 스크립트 전체가 막히던 잠재 위험 제거(defer+순서 재배치, mammoth 미로드 시 안내 가드) ②직전 패치에서 신규 함수 3종이 load 콜백 내부 지역 스코프에 갇혀 완전히 죽어있던 결함(최상위로 이동, node 검증에서 typeof undefined로 확인) ③드롭 이벤트가 실제로 handleResumeFile을 호출하지 않던 배선 누락(클릭 경로만 연결되어 있었음) 수정.
[v2.8.3] 2026-08-10 — 인력 프로필 등록 폼의 파일 드롭존이 실제로는 아무 이벤트도 처리하지 않아 브라우저 기본 동작(파일 열기/다운로드)이 발동하던 결함 수정: drop·dragover 기본 동작 차단 + 준비 중 안내로 문구 정정("자동 채움" 표현이 오해를 줌). 실제 자동 채움 기능은 Step3에서 구현 예정.
[v2.8.2] 2026-08-10 — 인력 프로필 화면 무동작 결함: S07_PROFILE·S16_PROFILE_ITEM 시트가 아직 생성되지 않은 상태에서 fetchSheet가 400 오류를 던지고 Promise.all 전체가 실패해 화면이 완전히 멈추던 결함. 수정: fetchSheetSafe(존재하지 않는 시트=null 반환, 다른 오류는 그대로 전파)로 안전화, 시트 없을 시 화면 상단에 안내 배너+[시트 자동 생성] 버튼(ensureSheet — addSheet API 호출 후 헤더 기록). 다른 화면은 기존 시트만 조회하므로 동일 결함 없음(전수 확인).
[v2.8.1] 2026-08-10 — 사업기회 확도 물리 확정: 수주·계약·종료 단계로 전환 저장 시 probability 컬럼값 자체를 100으로 시트에 기록(그 전엔 확도 입력칸이 기회 단계에서만 노출되어 이전 % 값이 그대로 남아있었음). v2.8의 계산상 확정 폴백과 별개로 원천 데이터를 정정.
[v2.8] 2026-08-10 — 추정 자금수지 확도가중 전면 재작성(사용자 4건 결함 신고 전부 확정): ①"확정" 단계 스케줄 미생성분에 probability를 무조건 100으로 강제 대입 → 사용자가 확도 0%로 설정해도 무시되던 결함(강제 대입 제거, 실제 확도값 사용) ②확도가중 대상(wRows)이 체크박스 상태와 무관하게 항상 confNoSch 포함 → 체크 on/off 결과 동일했던 결함(wRows 구성을 weighted 조건에 실결합) ③지출측이 견적/파이프라인 확도가중을 전혀 반영하지 않음(매입 스케줄 S05만 참조) → 입금만 부풀고 지출은 그대로라 연말 잔액 과다 누적 → 파이프라인 연결 견적서(S12)의 매출/매입 합계비로 원가율 산출, 매입도 동일 가중으로 대칭 반영(estRatio). 상세 모달에 "확도가중 매입 월할분" 낱개 표시 보강. 검증: 확도0%는 포함 여부 무관 유입 0, 확도50%는 미포함 시 0·포함 시 유입 5,500,000+유출 3,850,000(원가율 70% 정확 반영).
[v2.7-Step1·2] 2026-08-06 — 인력 프로필 관리 착수(승인 설계): S07_PROFILE(19c)·S16_PROFILE_ITEM(13c) 스키마, 설정 코드 관리 ④ 기술 태그(대표+동의어), 사이드바·더보기에 👥 인력 프로필. profile.html: 목록+검색(키워드=이름·기술·고객사·역할·업무·개발환경 통합, 필터=등급·연차·태그(동의어 수렴)·상태·가용일, 가용일 경과 ⚠), 상세(6섹션 표·연락처 마스킹·PDF 버튼 자리), 수동 등록/수정(6섹션 행 편집·스냅샷 롤백). Step3(docx 자동 매핑)·Step4(PDF 2종) 예정.
[v2.6] 2026-08-06 — 부가세 [자금수지 반영] 정식 경로 재정비: 구버전이 S02 est_vat만 직접 수정(잔액 미재계산·다음 재계산에 덮임 — 화면 무변동의 원인)하던 데드 경로 제거. 반영=S13에 확정 기록(cashflow_reflected·금액)만 하고, 확인 후 자금수지로 이동(?recalc=1)해 추정 재계산 자동 실행(비로그인 시 안내). 재계산은 기존대로 S13 납부기한 월에 실납부액(없으면 추정세액)을 유출로 집계 — 추정 산출자 단일화(SSOT).
[v2.5.9] 2026-08-05 — 매칭 정합 감사 확장: 보고된 화면 오표시의 실체=시트에 남은 "근거 없는 완료"(is_received=Y인데 matched_txn_id 없음)·"유령 거래 참조"(matched가 원장에 없음) — 중복 감사의 사각지대. 통장 대사 배너가 두 형태 추가 검출·나열, [근거 없는 완료 일괄 해제](발행·수취 보존, 재대사 복귀). 표시 로직은 재현 검증으로 정상 확인.
[v2.5.8] 2026-08-05 — 매칭 불변식(하나의 거래=최대 1건) 확립: ①수동 매칭 저장 시 이미 매칭된 거래ID 차단 가드(매출·매입) ②자동 매칭 실행 중 거래 소비 방어 ③중복 매칭 감사 배너(통장 대사 카드) — S04·S05의 matched_txn_id 중복 그룹 자동 검출·나열, [중복 정리 실행]으로 각 거래당 예정일 최근접 1건만 유지·나머지 해제(발행·수취 보존). 과거 오염(v2.4.2 이전 자동확정 사고 잔재) 정리 도구.
[v2.5.7] 2026-08-05 — 수동 매칭 후보 누락 수정: 동액 다월 청구 시 추천 후보가 ±45일·상위2 제한으로 잘리고 라벨이 금액뿐이라 월 구분 불가하던 것을 — 금액 일치(±1,000) 후보 전부·수금/지급예정일 오래된 순·라벨에 연월 표기(툴팁=예정일)로 개선. 콤보는 예정일 순 정렬+이미 매칭 건 제외(이중 매칭 방지), 매입 대칭. S04/S05 읽기 범위 300→1000행 확대(청구 누적 대비, schema SSOT 1곳).
[v2.5.6] 2026-08-04 — 파생 시트 동기화: S08_CASHFLOW_ACT가 업로드 시점 스냅샷으로 남아 원장(S08_DETAIL) 삭제 후에도 시트에 잔존하던 위생 문제 해결. CSV 저장·추정 재계산 시 ACT를 항상 원장 재파생본으로 재작성(writeActFromDetail) — 원장을 비우면 재계산 한 번으로 ACT 시트도 비워짐. 화면·원장·파생 시트 삼자 일치.
[v2.5.5] 2026-08-04 — 매입 수취 목록에 버튼 HTML이 텍스트로 노출되던 결함(v2.4.9의 수취 취소 삽입 앵커가 수정 버튼 여는 태그 중간이었음) 수정: 삼항 버튼들을 태그 밖으로 재배치, 수정 버튼 태그 복원.
[v2.5.4] 2026-08-04 — 실적 데이터 SSOT 확립: 실입금·실출금·실잔액이 파생 집계(S08_ACT)를 읽어 원천(S08_DETAIL) 삭제·수정이 화면에 반영되지 않던 정합성 결함 수정. 실적은 항상 S08_DETAIL에서 재파생(저장된 분류·확정 순서 사용), S08_ACT는 기록용으로만 유지. 원천을 지우면 실적도 즉시 사라짐. (추정 잔액이 CSV와 무관한 것은 v2.5 승인 설계 — 계획 순수 누적.)
[v2.5.3] 2026-08-04 — 모바일 하단 탭에 [⋯ 더보기] 추가(nav.js 단일 수정, 전 화면 적용): 고정비·부가세·설정 진입 경로 신설, 해당 화면에서는 더보기 탭 활성 표시, 팝업 외부 탭 시 자동 닫힘.
[v2.5.2] 2026-08-04 — 잔액 연속성 결함(동일 시간대 거래 순서 뒤섞임→잔액 신뢰 불가) 전면 수정: ①거래일시 시분초 보존(구 slice(0,10)이 시간 삭제하던 근원) ②orderAndChain: 시간 오름차순+동일 일시 그룹은 잔액 체이닝(직전잔액+입금−출금=잔액 연쇄)으로 순서 확정, 전 구간 연속성 검증 후 결과 표시(불연속 N지점 경고) ③확정 순서로 결정적 ID 부여, 병합 시 지문(일시|입금|출금|잔액) 대조로 기존 ID·수동 분류 보존(대사 링크 안전) ④월말 잔액=확정 시퀀스 마지막 거래. 복구=전 기간 CSV 재업로드(병합·재체이닝) 후 추정 재계산.
[v2.5.1] 2026-08-03 — 고정비 계획 단건 수정 기능: 목록 행 [수정] 클릭 → 모달에 값 로드(월·항목·금액·지급일·메모, 기간 전개는 신규 전용이라 종료월 잠금) → [수정 저장]으로 해당 건만 교체. 삭제·재등록 프로세스 대체.
[v2.5] 2026-08-03 — 추정 산식 개편(사용자 설계 채택): 추정 잔액=기초 잔액(설정: 기산월 자본금, 기본 2026-03/20,000,000)에서 시작해 월별 계획 수금(입금완료 포함)·지급·고정비·VAT를 순수 누적. 실잔액과 절대 혼합하지 않음(차이=계획외 이벤트, 누적 성질). 실잔액=월말 통장잔액(v2.4.7). 설정 화면에 기초 잔액·기산월 파라미터 신설, 상세 모달 산식·기산 전 표기 갱신.
[v2.4.9] 2026-08-03 — 세금계산서 수정 무반응 결함(v1.4 폼 접기 이후 수정이 접힌 폼에 값만 채우고 열지 않음 + 구 상단 스크롤 잔재) 수정: 수정 클릭 시 개별 폼 자동 펼침·해당 위치 스크롤. 상태 되돌리기 원클릭 신설: 발행완료(입금 전) [발행 취소]→발행예정, 수취완료(지급 전) [수취 취소]→수취대기.
[v2.4.8] 2026-08-03 — 견적 인건비 단가 수정 시 undefined 결함: v2.0 모듈화에서 estimate의 문자열형 fc가 common의 엘리먼트형 fc로 대체된 시그니처 충돌 — 입력칸 재포맷을 comma() 직접 호출로 교정(4곳). 단가 세트 기본값 + 수동 수정 정상.
[v2.4.7] 2026-08-02 — 실잔액이 월말이 아닌 월초 통장잔액으로 저장되던 결함 수정: 원인=잔액을 파일상 마지막 행에서 취하는데 하나은행 CSV는 최신순(내림차순)이라 월의 가장 이른 거래가 마지막에 처리됨. 교정=파일 정렬 방향 자동 감지 후 월 내 최신 일시 거래의 잔액 선택. 복구=전 기간 CSV 재업로드(병합) 후 추정 재계산 1회.
[v2.4.6] 2026-08-02 — [월별 추정 vs 실적] 행 클릭 상세에 "추정 잔액 구성" 섹션 신설: 전월 잔액 + 추정입금(수금예정 낱개 + 확도가중분) − 추정출금(매입 낱개 + 고정비 낱개 + VAT) = 잔액, 검산 일치 표시. 실잔액 앵커 월은 앵커 안내 표기.
[v2.4.5] 2026-08-02 — [월별 계획 vs 실적] 계획입금·계획출금 셀 클릭 시 건별 근거 모달(프로젝트·발생월·수금/지급예정일·VAT포함 금액·완료 표시·합계, 출금은 매입+고정비 구분) 신설 — 데이터 검증용.
[v2.4.4] 2026-08-02 — CSV 업로드 파싱 오류(과거 월 CSV 재업로드 시 요약 카드가 병합 최신 월을 이번 CSV에서 찾다 크래시) 수정: 유형별 요약 카드 기준을 이번 CSV의 최근 월로 교정.
[v2.4.3] 2026-08-02 — CSV 저장 병합 원칙: 업로드 저장이 현재 CSV만으로 S08·S08_DETAIL을 전량 교체해 과거 월 실적·거래가 유실되던 결함 수정. 월 실적=기존 보존+이번 CSV 등장 월만 갱신, 거래 상세=txn_id 병합(신규 우선). 복구=과거 CSV 재업로드(병합이라 안전).
[v2.4.2] 2026-08-02 — 통장 대사 결함: 매출 수금 자동매칭이 ①동액 다건이어도 첫 건 강제 확정 ②미발행 건 포함 ③CSV 재업로드 시 같은 입금 재매칭(→동액 3건 전부 입금완료 오염)이던 것을 매입과 대칭의 보수 원칙으로 교정(발행 완료 건만·후보 정확히 1건일 때만·사용된 거래ID 스킵, 매입에도 거래ID 가드 추가). 복구 기능: 세금계산서 화면 입금완료 건 [입금 해제]·지급완료 건 [지급 해제] 버튼 신설(매칭 정보만 제거, 발행·수취 보존).
[v2.4.1] 2026-08-02 — 대시보드 할 일 산식 결함 수정: ①매출 금액 0원 원인=S04에 존재하지 않는 amount 컬럼 참조(실컬럼: 공급가=tax_invoice_amt) → 공급가+vat_amt(없으면 10%) 합산으로 교정 ②매입 건수 누락 원인=당월 예정만 집계 → 과거 예정 미수취(이월) 포함, "(이월 N건 포함)" 라벨 ③검증용 건별 근거를 할 일 항목 아래 직접 표시(월·프로젝트·공급가+VAT=합계·예정일·이월 표시). 검증 스텁=실헤더(H4/H5) 원칙 확립.
[v2.4] 2026-08-02 — 정책 확정 반영: 사업기회 포기·실주 전환 시 연결 프로젝트의 계획 스케줄 자동 삭제(미발행 매출·미수취 매입만, 발행·수취·입금·지급 완료 건 보존). 삭제 전 건수 확인창, S14 변경 이력 기록.
[v2.3.1] 2026-08-02 — 세금계산서 목록이 포기·실주 사업기회의 청구까지 표시하던 결함 수정: okIds(확정~종료 프로젝트 집합)가 계산만 되고 필터 미적용(데드 변수)이던 것을 매출·매입 목록과 연도 옵션에 실결합.
[v2.3] 2026-08-02 — 대시보드 체크리스트: 미수금에 금액 표기 신설(경과 건 VAT포함 합, 미지급과 대칭), 미지급·미수·할일(매출/매입) 4개 항목에 "VAT 포함" 문구 명시, 발행할 매출 금액을 공급가→VAT포함(amount×1.1)으로 통일. [추가] index 마감월 KPI의 prevYmStr 선언 전 사용(TDZ, v1.2 잠복 — 실잔액 無월에 대시보드 전면 크래시) 선언 블록 상향 이동으로 해소.
[v2.2.1] 2026-08-02 — v2.2 중복 제거 시 동일 스타일 문자열 전체 치환으로 통장 대사 카드 제목의 정식 버튼까지 삭제된 결함 복원(openTxModal 트리거 버튼 정확히 1개 보장, 파란 강조 스타일).
[v2.2] 2026-08-01 — 자금수지: CSV 거래 내역 버튼 중복 제거(카드 제목 1곳으로), 거래 내역 모달에 [건별 내역|유형별 집계] 보기 전환 추가(유형별 입금·출금·건수 + 합계, 월 필터 연동).
[v2.1] 2026-08-01 — 코드 관리: ▲▼ 우선순위 이동 기능 추가(분류 검사 순서=행 순서), 사용 매뉴얼 PDF(docs/코드관리_매뉴얼.pdf) 신설 및 화면 링크 연결.
[v2.0.3] 2026-08-01 — 설정 화면 진입 오류 수정: v1.3에서 제거된 레거시 카드의 입력칸(kw_*)에 로드 코드가 값을 대입하다 null 참조로 중단되던 결함. 요소 존재 가드(안전 세터) 전면 적용.
[v2.0.2] 2026-08-01 — Sheets API 429(호출 한도)·5xx 자동 재시도(지수 백오프 3회)를 common.js fetchSheet에 추가. 반복 새로고침 시 일시 오류 자동 복구.
[v2.0.1] 2026-08-01 핫픽스 — schema.js 키 충돌(S08_CASHFLOW_ACT/S08_DETAIL이 동일 키 S08로 축약되어 자금수지 로드 실패) 수정: R.S08=실잔액, R.S08_DETAIL=거래상세 분리. html의 모듈 캐시 쿼리 ?v=2.0.1 승급.
[v2.0] 2026-08-01 아키텍처 개편(기능 변경 0) — js/common.js(설정·인증·API·유틸)·js/schema.js(시트/범위/헤더/상태 SSOT)·js/calc.js(MM·월할 엔진)·js/nav.js(사이드바9·탭바6 단일 렌더)·css/common.css 분리. SSOT 감사 13/13, 전 9페이지 실행 검증·MM 스펙 회귀 통과. 배포=html 9 + js/ 4 + css/ 1 (폴더 구조 유지, ?v= 캐시 무효화).
[v1.5] 2026-07-31 UI 표준화 — 세금계산서 목록 툴바 표준 신설(컨트롤 32px 통일·프로젝트 필터 신축·필터 초기화 버튼), 오배치된 초기화 버튼(폼 리셋 함수)을 폼 내부로 복귀.
[v1.4] 2026-07-31 재재테스트 잔여 3건 수정 — #8 스케줄 생성 버튼을 목록 카드로 승격(접힘에서 해방)·매출/매입 완전 대칭, #9 fillPrjFilters 미호출(데드코드) 연결로 연도·프로젝트 필터 실데이터 채움, #10 고정비 모달 PC 중앙 카드형 표준화+폼 간격 정상화.
[v1.3] 2026-07-29 재검수 5건 수정(#1할일분리·#4간트막대·#8,9필터·상태·접기·#10고정비 재구축·#11추천폴백·#13조회버튼·#14레거시 제거·S10범위 300). 이전: [v1.2] 2026-07-29 결함 15건 일괄 반영 — 세금계산서 관리(구 데이터입력)·고정비 계획(fixed.html 신설)·코드 관리(설정)·상시 통장대사·재계산 명세(확정 100%)·마감월 KPI·다년 간트 등. 상세: 결함관리대장_260729001_회신.xlsx
\xEF\xBB\xBF[HFC 웹 시스템 HTML 7종 — GitHub 저장소에 전체 덮어쓰기 업로드]

index.html    = 경영 대시보드
pipeline.html = 사업기회 관리 (7단계 상태 + 간트 보기)
estimate.html = 견적·시뮬레이션
input.html    = 데이터 입력 (계획매출/계획매입/고정비)
vat.html      = 부가세 관리
cashflow.html = 자금수지 (CSV 업로드)
settings.html = 시스템 설정 (단가세트/키워드)

※ 기존 저장소의 simulation.html 은 삭제하세요 (estimate.html 이 대체).
