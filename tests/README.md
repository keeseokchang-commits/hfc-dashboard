# HFC 회귀 테스트 스위트 (v2.9.49 기준, 2026-09-14 최초 구축·같은 날 확장)

## 실행 방법
```
cd v20
npm install jsdom          # 최초 1회(컨테이너 리셋 시 재설치 필요)
node tests/run_all.js      # 전체 스위트(현재 167개 케이스)
node tests/calc_mm.js      # 개별 파일만 실행도 가능
```

## v2.9.49 메모 (근본 재설계로 최종 확정)
CSV 거래 내역 팝업(v2.9.47에서 재지정 드롭다운 추가)의 가독성 문제(원칙38) — 저장·조회 로직만
검증하고 컬럼 폭·가독성을 설계하지 않아 긴 유형명("기타 지급(세금계산서)" 등)이 드롭다운에서
잘려 보이던 문제. 첫 시도(table-layout:fixed + colgroup, 유형 컬럼 112px 고정)는 옵션 텍스트가
문자열로는 안 잘렸지만 실제 렌더링 폭이 부족해 시각적으로는 여전히 잘려 보였다 — 컬럼 폭에
값을 맞추는 접근 자체가 근본 해법이 아니었다. 최종 수정: 6컬럼 표를 버리고 기존 "미분류·기타
재지정" 섹션과 동일한 카드형 레이아웃(정보 줄 + 드롭다운을 전체 폭 별도 줄로)으로 전면 재설계 —
표 컬럼 폭 제약 자체를 없애 어떤 길이의 유형명도 원천적으로 잘릴 수 없게 만들었다.
"대사" 컬럼(수금매칭/지급매칭 배지)의 역할: 이 거래가 이미 재무 프로젝트의 수금·지급 예정과
자동/수동으로 매칭되어 완료 처리됐는지 표시 — 통장 대사(reconciliation)가 끝난 거래임을
한눈에 보여주는 상태 표시.

## ⚠️ v2.9.48 — v2.9.43에서 놓친 VAT 이중계산 잔존 지점 발견 (반드시 읽을 것)
v2.9.43에서 계산서없음 매입(NOINV-)의 VAT 이중계산 결함을 grep으로 찾은 4곳에서 고쳤다고
판단했으나, 실제 사용자가 대시보드에서 매번 클릭하는 "N월 계획출금 구성" 팝업(openPlanDetail)은
완전히 별개의 함수라 검색에 걸리지 않았고 결함이 그대로 남아 있었다(원칙37). 사용자가 실제 화면
스크린샷 3장(8·9·10월)을 첨부하고 부산은행 매입이 정확히 1.1배 부풀려져 있다고 지적해 발견.
"같은 코드 패턴을 다 고쳤다"와 "그 결함이 나타나는 모든 화면을 확인했다"는 다르다 — 새로운
결함 패턴을 고칠 때는 그 데이터가 표시되는 모든 화면 진입점(모달, 팝업, 대시보드 배지 등)을
하나씩 나열하고 전부 열어서 확인할 것.

## v2.9.46~47 메모
- **v2.9.46**: 계산서없음 매입(급여형·사업소득형)의 지급일 규칙을 "익월말"에서 "전월 근무분
  당월 10일 지급"(회사 실제 급여일 규칙)으로 정정(원칙35). 부산은행 프로젝트의 8월 선지급분이
  9월 말로 밀려 8월 계획출금에서 누락되던 것을 실제 시트 데이터 대조로 발견.
- **v2.9.47**: "CSV 거래 내역" 팝업(건별 내역)에 재지정 드롭다운 추가 — 기존 "미분류·기타 재지정"
  UI는 그대로 유지하고, 이미 분류된 거래도 이 팝업에서 별도로 재지정 가능(원칙36). id 접두사를
  rc_/rct_로 분리해 두 UI가 동시에 렌더링돼도 값이 섞이지 않도록 함.

## ⚠️ v2.9.42~45 — 자금수지 값 왜곡 4연속 사고, 근본 원인은 "하드코딩 문자열 매칭" 자체였다 (반드시 읽을 것)
사용자가 "자금수지 값이 엉망이다"라고 지적한 뒤 네 버전에 걸쳐 원인을 찾아 고쳤다. 앞 세 개는
증상마다 다른 진짜 결함이었지만, v2.9.44는 원칙에 어긋난 편법이었다는 것이 핵심이다:
- **v2.9.42**: txnTypeNames()의 필드명 불일치(r.key/r.value → setting_key/setting_value, 원칙30).
- **v2.9.43**: planCost의 계산서없음 항목에 VAT를 이중 계산하던 결함(원칙32).
- **v2.9.44**(편법, 이후 폐기): "매출입금"과 "매출 입금"의 공백을 제거해 우연히 일치시켰다 —
  진단(자유 입력과 하드코딩 문자열의 불일치)은 맞았지만 해법이 "이번 증상만 가리는 변형"이었다.
  사용자가 등록한 실제 유형(보증보험·수수료·경조사비 등 이름도 개수도 하드코딩 9개와 다름)에는
  애초에 통하지 않는 미봉책이었고, "무조건 편법이라도 기능이 돌아가는 것처럼 보이게 하면 신뢰를
  받을 수 없다"는 지적으로 정정(원칙34).
- **v2.9.45**(근본 해결): 코드 관리 화면(①거래유형&키워드)에 "집계 버킷" 지정 드롭다운을 추가 —
  사용자가 등록한 유형이 어느 집계 카테고리에 속하는지 명시적으로 지정하고(S10의 description
  컬럼에 저장), 자금수지는 하드코딩 문자열과 비교하는 대신 이 매핑을 조회한다. 유형명이 무엇이든,
  몇 개를 등록하든 항상 정확하다.
사용자 자유 입력을 하드코딩 문자열로 다시 알아내려는 구조를 발견하면, 문자열 비교를 정교화하지
말고 등록 시점에 명시적 매핑을 받도록 구조 자체를 바꿀 것(원칙34). 그리고 화면 증상만으로 반복
추정하지 말고, 연결된 도구가 있으면 원본 데이터를 직접 읽어 확인할 것(원칙29·33).

## v2.9.39 → v2.9.40 설계 정정 메모
v2.9.39에서 "CSV 자동분류 키워드"·"거래점 분류 규칙" 두 카드를 복원했으나, 사용자 확인 결과 이
UI는 삭제가 실수가 아니라 의도적인 결정이었다 — 현재의 코드 관리(①②③) 세 가지 입력 패턴만으로
충분하다는 것이 최종 확정. v2.9.40에서 그 두 카드를 다시 제거했다. 대신 그 조사 과정에서 훨씬
중요한 결함(원칙30 — S10 필드명 불일치로 코드 관리 데이터가 자금수지·고정비 화면에 전혀 반영되지
않던 것)을 발견해 수정했다.

## ⚠️ v2.9.37~38 — S10(설정) 시트 관련 실제 사용자 데이터 유실·오염 사고 (반드시 읽을 것)
S10_SETTINGS 한 시트를 settings.html의 여러 함수(loadSettings/saveSettingsToSheet/loadCodes/
saveCodes)와 input.html의 markScheduleChanged가 함께 다루는데, 이 여러 지점에서 서로 다른 결이지만
근본적으로 같은 유형의 결함이 두 버전에 걸쳐 발견됐다:
- **v2.9.37**: markScheduleChanged가 S10 읽기 실패 시 "원래 비어있었다"로 오인해 시트 전체를
  덮어씀 → 코드 관리 데이터 전체 삭제(PRINCIPLES.md 27번, 최우선 원칙).
- **v2.9.38**: loadSettings가 코드 관리 카테고리(txn_type 등) 행까지 구분 없이 settingsMap에
  담고, saveSettingsToSheet가 그 settingsMap 전체를 "이게 전부다"로 착각해 시트를 덮어쓰며 코드
  관리 데이터의 category까지 오염시킴 — "키워드 저장" 버튼 하나로도 발생(PRINCIPLES.md 28번).
둘 다 사용자가 그 시트를 정상적으로 계속 쓰는 동안은 드러나지 않다가 특정 조건에서만 터지는
유형이었다. 앞으로 여러 함수가 한 시트를 나눠 관리하는 구조를 새로 만들거나 수정할 때는
e2e_data_loss_guard.js를 먼저 참고하고, ①읽기 실패 시 쓰기 포기(27번) ②자신이 모르는 카테고리는
읽기·쓰기 양쪽에서 명시적으로 보존(28번) 두 가지를 반드시 확인할 것.

## 왜 만들었나
이 시스템은 결함을 고칠 때마다 즉석 스크립트로 검증하고 버리는 방식이었다. 그 결과 같은 영역을
다시 건드릴 때마다 검증 코드를 처음부터 새로 짜야 했고, "고쳤는데 다른 게 깨졌다"는 사고가
반복됐다. 이 스위트는 지금까지 실제로 발생했던 결함 전부를 재현 가능한 테스트로 고정해,
앞으로 뭔가 고칠 때 "기존이 안 깨졌는지"를 30초 안에 확인할 수 있게 하는 것이 목적이다.

## 구조
- `calc_*.js` — 순수 함수 계층(schema.js·common.js·calc.js)만 vm으로 로드해 검증. DOM 없이 빠름.
- `e2e_*.js` — 실제 화면(HTML)을 jsdom으로 띄워 클릭·입력 이벤트까지 재현. 느리지만 화면 조작
  순서에서만 드러나는 결함(체크박스 상태 꼬임 등)은 이 방식이 아니면 못 잡는다.
- `_helpers.js`, `_e2e_helpers.js` — 공용 유틸(테스트 파일로 취급 안 됨, run_all.js가 스킵).
- `run_all.js` — 전체 실행기.
- `PRINCIPLES.md` — 이 결함들에서 뽑아낸, 프로젝트에 종속되지 않는 일반 원칙(스킬화 재료).

## 새 결함을 고칠 때 반드시 할 것
1. `node tests/run_all.js`로 기존이 안 깨졌는지 먼저 확인
2. 고친 결함을 검증했던 스크립트를 버리지 말고, 해당 영역의 `tests/*.js` 파일에 케이스로 추가
   (새 화면/새 영역이면 새 파일 생성 — `calc_*` 또는 `e2e_*` 명명 규칙 따를 것)
3. 다시 `node tests/run_all.js`로 전체 재확인
4. zip 패키징 시 `tests/` 디렉토리 전체를 포함할 것(다음 세션·다음 사람이 이어받을 수 있게)

## 현재 커버 범위 (167개 케이스, 18개 파일)
| 파일 | 커버하는 결함/원칙 |
|---|---|
| calc_mm.js | MM 일할계산 실무 공식(v2.9.19), 3인 예제 스펙 |
| calc_types.js | 콤마 타입 안전성(v2.9.9), 자유형식 컬럼 보존, S06 신규 필드 |
| e2e_schedule.js | 저장버튼 undefined(v2.9.10), 매입 직접입력 타이밍버그(v2.9.16), _EMPTY 마커 오염(v2.9.14). ※ v2.9.27의 "매입 스케줄 전 지급형태 포함" 설계는 v2.9.36에서 폐기·재정정(세금계산서형만) — 아래 참고 |
| e2e_confirmed_data.js | 확정 데이터 보호 원칙, 마일스톤 저장·복원(v2.9.12), 매입 대상 0건 시 저장버튼 잠김 방지(v2.9.25, 견적삭제 시나리오로 갱신) |
| e2e_estimate.js | 빈 상태 마커(v2.9.13), 지급형태 구분(v2.9.17), 급여형 이중계산 방지(v2.9.18), 급여형 월별 지급액(v2.9.23), 매출·매입 단가×MM 대칭 UI 구조(v2.9.30~31) |
| e2e_project_buyprice.js | S12.buy_price="단가" 저장 계약을 project.html·cashflow.html이 지키는지 감시(v2.9.31 근본 결함 재발 방지) |
| e2e_ui_symmetry.js | 매출·매입 UI 대칭 표준(v2.9.32) — 청구스케줄 미리보기 표 컬럼 구조. v2.9.33: 수취완료 처리 시 지급예정일 자동계산(동작 레벨 대칭). v2.9.34: 매입 목록 카드에 공급가액+VAT+합계(VAT포함) 라벨 표시(실제 지급총액 오인 방지). v2.9.36: 계산서없음 건은 세금계산서 목록에서 완전히 제외됨을 확인(v2.9.34 당시 표시 방식에서 재정정). ※ 개별등록 폼 관련 케이스는 v2.9.35에서 폼 자체가 삭제되어 e2e_etc_noinvoice.js로 이관·정리 |
| e2e_etc_noinvoice.js | v2.9.35 — 기타원가(인프라·솔루션·기타) 세금계산서 수취 여부 체크박스, 개별입력 폼 완전 삭제 및 그로 인한 loadAll() 회귀(H4/H5 상수·rev_project/cost_project DOM 참조 삭제 사고 재발 방지). v2.9.36: 청구 스케줄 매입기준(eBuy)에서 계산서없음 완전 배제 확인 |
| e2e_cashflow_noinvoice.js | v2.9.36 — 자금수지가 계산서없음 매입(사업소득형·급여형·미수취 기타경비)을 견적(S12)에서 직접 월별 계산해 반영, 세금계산서형(S05)과 혼재 시 이중계산 없음, 견적이 전부 세금계산서형이면 과다생성 없음. v2.9.43 — planCost 소비 지점(계획출금 계산·상세 모달 표시)이 계산서없음 항목에 VAT를 이중 계산하지 않는지 확인(원칙32). v2.9.46 — 지급일이 정확히 "익월 10일"(회사 급여일 규칙)로 산정되는지 연도 경계 포함 확인(원칙35) |
| e2e_csv_reclass_popup.js | v2.9.47 — "CSV 거래 내역" 팝업의 재지정 드롭다운(이미 분류된 거래도 재지정 가능), 미분류 재지정 UI와 동시 렌더링 시 값이 섞이지 않는지 확인(원칙36) |
| e2e_openplandetail_vat.js | v2.9.48 — "N월 계획출금 구성" 팝업(openPlanDetail)이 계산서없음 매입에 VAT를 이중 계산하지 않는지 확인(원칙37, v2.9.43에서 놓친 잔존 결함) |
| e2e_csv_popup_readability.js | v2.9.49 — CSV 거래 내역 팝업이 카드형 레이아웃(표 컬럼 폭 제약 없음)으로 재설계되어 긴 유형명도 잘리지 않고, 재지정 저장 기능도 정상 동작하는지 확인(원칙38) |
| e2e_cashflow_whitespace.js | v2.9.45 — 코드 관리 화면의 "집계 버킷" 명시적 지정(설정 저장·재조회) 및 자금수지가 이 매핑으로 정확히 집계하는지 확인(원칙34, v2.9.44 편법을 폐기한 근본 재설계) |
| e2e_data_loss_guard.js | **[최우선]** v2.9.37 — S10·S17 읽기 실패 시 기존 데이터 보호(원칙27). v2.9.38 — 코드 관리 카테고리 오염·삭제 방지(원칙28). v2.9.40 — CSV 키워드 UI 재제거 확인, 고정비 추가 버튼 위치 수정, ①②③ 코드 관리 데이터가 필드명 통일 후 자금수지·고정비 화면에 정확히 반영되는지 확인(원칙30). v2.9.42 — txnTypeNames() 필드명 불일치 수정 확인(원칙30 계열) |
| e2e_fixed_recon.js | v2.9.41 — 고정비 통장 대사(매출·매입과 대칭 기능, 원칙31) — 자동/수동 매칭, 지급완료 배지·해제, 다른 건 수정 시 대사 정보 보존(fixed.html 헤더 하드코딩 결함 수정 포함) |
| e2e_vat.js | 부가세 확정값 잠금(v2.9.21), 기타경비 매입세액 반영(v2.9.22), vat_amt=0 폴백 오류 방지(v2.9.27) |
| e2e_cashflow.js | 추정 재계산 필요 배너(v2.9.20) |
| e2e_nav_version.js | 전 화면 헤더 배포 버전 배지 표시(v2.9.28) — 헤더 클래스명 비일관성(app-logo/app-title)으로 인한 누락 방지 |

## 설계가 두 번 뒤집힌 이력(v2.9.24 → v2.9.27 → v2.9.36)에 대한 메모 — 반드시 읽을 것
- **v2.9.24**: "매입 스케줄 생성 대상=부가세 매입세액공제 대상"으로 설계 → 계산서없음(사업소득형·
  급여형)을 매입 스케줄에서 완전히 제외.
- **v2.9.27**: "매입 스케줄(자금유출 추적)은 지급형태 무관 전원 포함, 부가세 대상 여부는 각 건의
  vat_amt로만 구분"으로 반전(PRINCIPLES.md 18번) — 계산서없음도 "세금계산서" 화면의 매입 목록에
  포함시키되 부가세만 0으로 표시.
- **v2.9.36(현재)**: v2.9.27을 다시 반전 — "세금계산서" 메뉴는 세금계산서 관리에만 집중해야
  하므로(사용자 지적: 계산서 없는 거래에 "공급가액"·"수취예정" 같은 세금계산서 용어를 붙이는 것
  자체가 전제가 틀림, PRINCIPLES.md 26번) 청구 스케줄 생성은 세금계산서형만 만들고, 계산서없음의
  지급 예정은 자금수지(cashflow.html)가 견적(S12)에서 직접 계산해 반영한다. **현재 유효한 설계는
  v2.9.36이다** — 과거 버전의 README.txt 항목(v2.9.24, v2.9.27 시점 커밋 메시지)을 참고할 때는
  이 두 번의 반전을 반드시 인지하고 읽을 것.

## S12.buy_price의 "단가" 계약(v2.9.26~31)에 대한 메모 — 중요
S12_ESTIMATE.buy_price는 **급여형만 예외**(월별 확정 총액을 memo의 JSON으로 관리)이고, **세금계산서형·
사업소득형은 항상 "단가"**다 — project.html·cashflow.html·input.html의 monthlyProfile이 전부 이
전제로 mm을 곱한다. v2.9.26에서 사업소득형의 이중계산을 막으려 이 필드에 "총액"을 넣는 방향으로
바꿨다가, 그 필드를 읽는 다른 3개 화면과 계약이 어긋나 정확히 mm배만큼 원가가 부풀려지는 결함이
발생했다(v2.9.31에서 발견·원상복구, PRINCIPLES.md 20번). buy_price를 다루는 코드를 또 고칠 일이
생기면, 반드시 이 계약("급여형만 총액, 나머지는 단가")을 먼저 확인하고 project.html·cashflow.html·
input.html 세 곳 모두에 영향이 없는지 점검할 것 — e2e_project_buyprice.js가 이 계약의 감시용이다.

## 아직 커버 안 된 영역(다음에 추가할 것)
- 이력서 자동 매핑(v2.9~v2.9.4, profile.html) — 실샘플 파일 의존이라 별도 fixture 필요
- 통장 대사(v2.4.2~v2.5.9 계열) — 아직 이 스위트 구축 시점엔 미착수
- 데이터 타입 표준화(v2.9.9) 중 나머지 화면(pipeline, settings, fixed, profile, index, project)의
  rowsFor/num 적용 — calc_types.js는 스키마 레벨만 검증, 화면별 E2E는 미비
